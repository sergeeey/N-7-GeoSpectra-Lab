# Predictions recorded BEFORE Tom Lawrence's reply

**Written:** 2026-08-10
**Reply expected:** user's personal check-in, Wednesday 2026-08-12
**Mode:** `boyko-scientific-consortium` — "Предсказания-до-данных"

**Why this file exists and why it is dated.** This is the one analysis mode that
cannot be run retroactively. A prediction written after seeing the answer is a
rationalisation; the only thing that makes it a prediction is that it is on disk
first. Standing constraint respected: **we do not initiate contact with Tom** —
this records what each answer would mean, it does not solicit one.

---

## The question, at its CURRENT state (not the 2026-07-05 version)

The pearl queued for this contact was written 2026-07-05 and asks:
*"does Tom's framework carry an independent fiber Spin(8)?"* — **that target no
longer exists in that form.** Rounds 119-128 moved it:

- **Gate 1 is CLOSED.** Algebraic distinguishability of all three channels
  `8_v/8_s/8_c` is achieved by two structurally independent routes —
  `SO(4)×SO(4)` block-chirality (round119) and `su(3)⊕u(1)⊕u(1)` (round124) —
  plus an explicit verified isomorphism `ℂ⊗8_v ≅ Σ` (round127/128, residual
  ~1e-15, exhaustive over all 12 members of `Aut(su(3))`).
- **The live blocker is narrower:** does that rank-4 structure act **globally on
  the compactification** (not just on the fibre), and does the physical Dirac
  operator survive the `G₂`-breaking both candidates require?
- A second pearl (2026-07-15, `RESOLVED`) closed two more doors — the
  representation-ring route and the `G₂`-invariant-connection route — leaving,
  in its own words, *"the ONLY remaining door: a genuinely non-invariant /
  symmetry-breaking extension (explicit background Φ, `D_E → D_E + Φ`)"*.

---

## The four worlds, and what each does to `C4_NGEN3` (currently CONDITIONAL)

| # | if Tom's answer indicates… | world | `C4_NGEN3` becomes | next action |
|---|---|---|---|---|
| **M1** | fermions carry an independent gauge/frame index **beyond** the S⁶ geometry | global action available | **PROVED** (still conditional on C27, untouched) | close the Schur argument, open round129 |
| **M2** | fermions are strictly geometric — associated to the S⁶ frame bundle | fibre-only | stays **CONDITIONAL** | pursue the `Φ` mechanism (the 07-15 pearl's remaining door) |
| **M3** | nothing about a third channel, but an explicit background / flux **is** present | Φ-world | stays **CONDITIONAL**, target moves | re-aim OB4 at `Φ`, not at rank-4 structures |
| **M4** | three generations are an **input** to his construction, not an output | reverse causation | ⚠️ **downgrade of wording**, not of mathematics | revise "derived" → "consistent with" in preprint |

**M3 and M4 are not in the queued pearl.** M4 in particular is the reverse-causal
branch from the causal map: *"Tom postulated 3 generations, then selected a
structure"* — and it changes what this project may claim without changing a
single computation.

## Falsifiable content of this record

If, after the reply, the observed answer is classified into a world **not**
listed above, this record FAILED as a prediction and must be marked so — not
retrofitted with a fifth row. Four worlds, stated in advance, is the bet.

## What this record does NOT do

1. Does not solicit anything from Tom — the user's check-in is personal and
   unprompted by this file.
2. Does not touch `C27` (multiplicity-2). That blocker is logically independent
   and none of M1–M4 moves it.
3. Silence, or an answer that does not address the fermion sector, is **not**
   evidence for any world (ICE strategy: while-active — conclude only from text
   actually received).
4. Does not establish that `SO(4)×SO(4)` or `su(3)⊕u(1)⊕u(1)` is the structure
   Tom would recognise; he may use different language for the same object, or a
   different object entirely.

## One test that needs no reply at all

From the counterfactual mode: `G74A` Lemma B is stated to require **exact** `G₂`
and to "not degrade gradually". That is a statement about *that proof*, not
necessarily about the fact. **Is there a quantitative version of Lemma B with a
margin under `G₂`-breaking?** If yes, both rank-4 candidates pass without needing
Tom at all, and this whole blocker dissolves. Cheap, internal, untried.
