# Round118 — Claim

**Gauge/Hilbert/Triality closure program, item 3.** Tests the user's own
proposed parent hypothesis:

> `H_physical = H_matter ⊗ H_generation`, where the found `S⁶`-kernel
> lives in the generation factor; `(4,4̄)` live in a separate matter
> factor; triality acts on the generation factor; the gauge group acts
> on the matter factor.

Proposed to explain simultaneously: why `k_vec` is an `SU(4)` singlet;
why `B-L=0`; why the current mode is not Pati-Salam matter; why the three
generation labels can be separated from gauge charges.

**User's own pre-registered kill-test** (used verbatim, not weakened):
the hypothesis dies if (a) the matter factor has to be chosen to match
the already-known Standard Model; (b) the full Dirac operator doesn't
act consistently on the product; (c) triality merely triples the whole
matter spectrum; (d) a spurious mirror spectrum appears; (e) there is no
new prediction.

## L0 gate (EstimandOps)

**Question type: Descriptive**, with an explicit warning against
smuggling in a Causal claim. The question "does a tensor factorization
`H_physical=H_matter⊗H_generation` **exist and is it forced** by the
already-established geometry" is descriptive/structural. The claim's own
motivating list ("this could explain why...") is an INTERPRETIVE
consequence, not itself the thing being tested — kept explicitly separate
below, per the Structure-Bias Guard (reason about the structure first,
interpret only after).

## Falsifiable claim

For the hypothesis to be a genuine **derivation** (not the user's own
kill-test (a), a post-hoc fit), it requires an independently-identifiable
`H_matter` — a nontrivial vector space, distinct from the already-computed
twisted kernel, carrying genuine `SU(4)`-representation content
(`(4,4̄)`), that this project's own geometry supplies **without being
defined circularly as "whatever carries the known SM content."**

**Pre-registered structural check, before any interpretation:** the
currently-established twisted kernel (round59/dolan-casimir,
`dim ker(D_{S⁶,twisted})=1` **per channel**, round107: this exact 1-dim
space is a genuine, full `SU(4)`-singlet) is **1-dimensional**. A
1-dimensional vector space cannot factor as a NONTRIVIAL tensor product
of two spaces (`V=A⊗B` with `dim(A),dim(B)>1` forces `dim(V)≥4`) — so IF
`H_generation` is meant to be (or contain) this exact already-computed
kernel, there is no room, dimensionally, for a nontrivial `H_matter` to
coexist WITHIN it. `H_matter` would have to live somewhere else entirely
— a genuinely new, not-yet-constructed space — not a re-reading of the
existing 1-dim kernel.

## Kill criterion (pre-registered, extending the user's own 5 conditions)

- If NO independently-derivable candidate for `H_matter` (carrying
  genuine `SU(4)` `(4,4̄)` content) can be found anywhere in this
  project's own established geometry — check specifically against gate
  G97 (rounds 102/108/109: **no `SU(4)` gauge-algebra realization exists**
  within `Iso(S³×S⁶)`, closed three independent ways within the standard
  product-manifold framework) — then the hypothesis is **BLOCKED at the
  construction stage**, not merely "not yet derived": it requires an
  object (`SU(4)`-representation-carrying `H_matter`) that a firmly
  established no-go already rules out geometrically, for the SAME reason
  the whole Pati-Salam program has been blocked all session.
- If a candidate `H_matter` IS found that does not require the ruled-out
  `SU(4)` gauge realization (e.g. an `SU(4)` REPRESENTATION label without
  an accompanying GAUGED `SU(4)` symmetry) — check whether this satisfies
  the user's own kill-test (a): is it independently motivated, or
  selected because it's known to work? Report honestly either way.

## What this does NOT mean (pre-registered)

1. Does NOT claim to have exhaustively searched for every possible
   `H_matter` candidate — checks the most directly relevant, already-
   established no-go (G97) first, as the cheapest differentiating test.
2. Does NOT affect `N_gen=3`, `lambda=FREE_COUPLING_PARAMETER`, or
   `safe_for_runtime=False`.
3. Does NOT re-derive round59/round94/round107's own established results
   (kernel dimension, B-L=0, SU(4)-singlet) — reused by citation.
