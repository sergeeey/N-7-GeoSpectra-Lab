# E1 — Decision

## Result: PASS (with a correction, not a contradiction)

All three load-bearing usages checked (Lawrence spin-connection mechanism,
Freund-Rubin flux, KT-8's product Dirac operator) require the **same** role:
**Role 1 — S³ is a genuine physical internal KK-compactification factor**,
not a gauge-fibre, not a parameter space, not part of the finite spectral
geometry. No inconsistency between usages was found — the kill criterion did
not trigger.

Roles 2/3/4 are not actually used anywhere in the paper's substantive
physics. Role 2 (gauge-fibre) was the a priori most plausible alternative —
it would make "10D" (4+6) literally consistent — but the Freund-Rubin flux
section (`preprint.tex:973-982`) is decisive against it: a gauge-fibre has no
independent volume for a 3-form flux to thread, and no independent spinor KK
tower for KT-8's product-operator argument to apply to. Both of those are
used as real, load-bearing physics elsewhere in the paper, not as incidental
language.

## What this closes

The correct statement is: **the physical spacetime is 4+3+6 = 13-dimensional**
(4D external × S³ × S⁶), and the recurring "10-dimensional" language
(`preprint.tex:189,289,973`) is a **mislabeling**, not evidence of a coherent
alternative reading. The likely origin of the error: conflating the
*dimension of the SO(4) spinor representation* on S³ (4 complex components,
`preprint.tex:289`) with a *spacetime dimension count* — 4 (rep dim) + 6
(S⁶) = 10 is an arithmetic coincidence between two unrelated quantities (a
representation dimension and a manifold dimension), not a real total.

## What this does NOT close

This is the same substantive finding the external RDR-2.1 review's own "KT-1"
(dimension mismatch) raised earlier this session — that review was
acknowledged as "confirmed as a textual problem, deeper than it looks" but
was not itself integrated into the preprint (this session's KT-1/KT-8
integration work addressed the *twist origin* and *full-operator zero-mode*
items, not the dimension label). E1 adds a firmer basis for this
correction (Freund-Rubin consistency check specifically), so it should now be
acted on, but has not yet been written into `preprint.tex`.

## Recommended next action

Add a `preprint.tex` open-problems item (or a direct correction, pending
user's choice of scope) stating: total dimension is 13, not 10; "10D"
language should be replaced with "9D internal (S³×S⁶) + 4D external" or
similar throughout; this does not change any established math result (index,
kernel, KT-8's conclusion), only the paper's own bookkeeping of what
dimension it is claiming to work in — which in turn affects what "parent
action" (KT-1) should even be sought in (a 13D theory, not "10D/11D" as
KT-1's own text currently phrases the search).

## Feeds into

E2 (D^(t) deformation family) and E3 (S³-twisted escape gate) should both be
framed under Role 1 (S³ as a genuine physical factor with its own Dirac
operator) — confirmed by this result, no reframing needed for either.
