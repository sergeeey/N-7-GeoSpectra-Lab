# C120 claim -- gate-fill check on the externally-proposed "I9"
t-selector, before any heavy computation

## Question type (EstimandOps L0)
**Descriptive.** Does a specific, externally-proposed candidate
construction clear `PARENT_ACTION_GATE.md`'s F4 pass criterion, given
already-established facts already in this project's own record? No
causal or predictive claim.

## Provenance (Gate 1 -- artifact identity)

The candidate ("I9") was described to the user, this session, by a
DIFFERENT (sibling) research project's own summary of its own
"H-19" investigation -- not read directly from that project's files,
not independently verified beyond what the user pasted in chat. Per
this project's own external-agent-findings-gate convention
(`feedback-external-agent-findings-gate`), the external session's own
[VERIFIED] is this project's [HYPOTHESIS] until re-checked here. As
literally described:

> тест `I9 ∝ (2t−1)·Vol(S³)·Vol(S⁶)` -- дешёвый, ~день работы,
> некруговой критерий `|a|=2|b|` для отбора `t∈{0,1}`, использующий
> уже сертифицированные данные N-7 и его же собственное правило
> (OB13: отбирающий член обязан быть нечётным по кручению).

No further formula, derivation, or source was supplied for `a`, `b`,
or the `|a|=2|b|` criterion -- these labels do not correspond to any
named quantity in this project's own `experiments/20260615-s6-harm-g1`
`I1`/`I2`/`I3` numbering (checked: that round's `I1=1/5, I2=1/3, I3=2`
are dimensionless volume-integral coefficients, unrelated to torsion
`t`). Treated as the external session's own, unverified labeling.

## Falsifiable claim

Before spending any computational effort building or numerically
testing `I9`, check whether it clears `PARENT_ACTION_GATE.md`'s F4
pass criterion: **"names a mechanism NOT already in the above list, or
explicitly names which item above it is extending and states the new
structural argument that distinguishes it from the already-failed
version."** F4's specific requirement, restated from F6: an actual
action principle must be named and its equations of motion derived or
cited -- not merely a hand-built odd function.

**Pre-registered prediction:** `I9`, as literally described (a bare
product of two already-known, torsion-independent volume factors times
an ad hoc `(2t-1)` multiplier, with no cited or derivable action
principle producing that specific combination), FAILS F6 for the same
reason several prior F4 attempts failed -- OB13 already establishes
that "odd in torsion" is a NECESSARY but NOT SUFFICIENT condition
(any of infinitely many odd functions satisfies it trivially; the
documented hard part, per `OPEN_BLOCKERS.md` OB1's own history, is
independent physical/action-principle motivation for THIS SPECIFIC
odd function over any other).

**Kill criterion for this round's own prediction:** if a genuine
derivation connecting `(2t-1)` to an actual named action term (Einstein-
Cartan torsion action, Chern-Simons/BF-type torsion term, spectral
action, or another explicitly cited framework) can be found or
constructed from this project's own already-established results, this
round's prediction is wrong and `I9` (or a corrected version of it)
should proceed to a full computational test.

## What this round does NOT show

- Does not test any OTHER F4 candidate not literally matching this
  description.
- Does not rule out that a PROPERLY MOTIVATED odd-in-torsion,
  volume-weighted term could exist -- only assesses the specific,
  as-described `I9`.
- Does not change N_gen=3's CONDITIONAL status; does not touch S6/
  triality/OB1's own PARKED status by itself (a gate-fill check is not
  a gate decision, per PARENT_ACTION_GATE.md's own scope note).
- Does not solicit Tom Lawrence's Part 5.

## Verification plan

- Check `OPEN_BLOCKERS.md` OB1's own prior-attempt register and
  `PARENT_ACTION_GATE.md` F4's already-tried list for anything
  structurally equivalent (duplicate check, per this project's own
  null_results/parked pre-check discipline).
- Search this project's own action-principle-adjacent rounds (Freund-
  Rubin G54A, spectral action G28/G29, Einstein-Cartan work) for any
  existing odd-in-torsion, volume-weighted term.
- If no such derivation exists: verdict is a gate-level PARTIAL/FAIL,
  not a computational result -- report honestly, do not round up to
  "tested and killed" (this round does not build or diagonalize
  anything; it is a prerequisite check, same shape as C107's own
  "is this the same object?" prerequisite round).
- FL Step 8a skeptic pass on this round's own gate-fill conclusion.
