# C120 decision -- externally-proposed "I9" does not clear the gate
as literally described, but the reasoning for that was wrong; skeptic
reconstructed a likely real referent AND surfaced a genuinely
promising, unexplored candidate this round's own criteria asked for

**Verdict:** `F6_NOT_SUPPLIED_UNCONDITIONAL__F4_INSUFFICIENT_INFORMATION__PROBABLE_REFERENT_RECONSTRUCTED_POINTS_TO_ROUND116_NOT_ROUND115__NEW_UNEXPLORED_CANDIDATE_SURFACED`
(replaces the original `F4_GATE_FAIL__SAME_SHAPE_AS_ROUND115_CIRCULARITY__NO_NEW_ACTION_PRINCIPLE_CITED`
-- corrected in place, FL Step 8a skeptic pass, same day)
**Status:** RESOLVED on the literal "I9" question; SUPERSEDED by a
sharper reconstruction; a new, unattempted lead recorded as a pearl.

## Summary

This was a pure gate-fill/literature-comparison check (no computation
run) on an externally-described candidate, `I9 ∝ (2t−1)·Vol(S³)·Vol(S⁶)`,
"non-circular criterion `|a|=2|b|`", with `a`,`b` undefined in the
source description. Original verdict: fails the gate, same shape as
round115's already-documented circular quantization mechanism.

FL Step 8a skeptic (context-blind) found the TOP-LINE conclusion
survives (I9 as literally, minimally described does not clear F6) but
the ARGUMENT for it was wrong, and -- more valuably -- reconstructed a
high-confidence hypothesis for what `a`/`b` actually refer to, which
points at a DIFFERENT already-tried duplicate (round116, not round115),
and separately surfaced a real, unexplored, well-motivated candidate
this round's own F6 criterion was looking for and failed to find due
to an incomplete search.

## Skeptic response (2026-08-31/09-01, FL Step 8a, context-blind)

Verdict: **WEAKENED**. Full response per the project's own Response
Matrix:

| Concern | Response |
|---|---|
| The "3 free parameters, therefore MORE circular than round115" argument ignores this project's own already-established constraint surface: G51 (`S_spec` monotone along `ρ₃=0.986ρ₆²`, the SM coupling constraint) and G57 (the UV ray meets the SM parabola at exactly one point) -- under these, there are 2 free parameters (t, ρ₆) at most, matching round115's own count, not 3; under BOTH, zero free radii | **Accepted, fixed below.** The parameter-count argument is corrected. |
| Symbol collision: this file used "F3" for both "PARENT_ACTION_GATE's torsion-family field" and "G54A's own finding" in adjacent lines -- exactly the Type-1 symbol-overload failure mode this project's own methodology names as an open gap | **Accepted, fixed** -- G54A's SM constraint finding is cited as G54A/G51/G57 below, not "F3", reserving F3 exclusively for the gate field. |
| Under the FULLY constrained reading (both G51 and G57 imposed), `I9` reduces to a function of `t` ALONE, and it is LINEAR in `t` -- its only zero is at `t=1/2` (the WRONG value, the Levi-Civita point KT-8 already showed has no zero mode), and it is unbounded if read as something to extremize. This is a sharper, radius-independent kill than "circularity" | **Accepted, this is now the correct kill for the literal formula, replacing the round115-circularity framing.** |
| `Vol(S³)·Vol(S⁶) = Vol(S³×S⁶)`, the total internal 9-volume -- NOT an exotic combination with no precedent. It is the a₀/Seeley-DeWitt term of the very spectral action this project already uses (G28: `g₂²∝Vol(S⁶)`, `g₃²∝Vol(S³)`, both volumes as coefficients in ONE action) | **Accepted, corrected.** "No precedent for the product" was wrong; the product is the single most standard volume-weighted action term available. What genuinely has no precedent is the odd `(2t-1)` FACTOR specifically. |
| Comparing `I9` to round115 on FORMULA alone (odd multiplier x volume factor) without filling the other 5 fields of `feedback-mechanism-transfer-gate`'s 6-field check (OBJECT, ROLE, GENERATOR, SECTOR, OBSERVABLE) is exactly the round86-89 lesson this project already learned, applied backwards -- there, "formula matched, mechanism didn't transfer" blocked a PROMOTION; here, formula-match alone was used to justify a KILL | **Accepted as the single strongest objection.** A shape-match is not sufficient evidence for "this is a duplicate" any more than it is for "this is confirmed." |
| F6 search had citation errors: G60 mischaracterized as Chern-Simons-adjacent (it is actually the Minkowski-uplift/moduli-stabilization gate, unrelated); G52 could not be located in any of 4 checked registries (possible phantom/typo citation); G34 conflates 3 sub-gates (D1/A2/B3), only B3 is CS-adjacent; rounds 101 and 104 are the project's OWN explicitly-declared SAME finding, cited as if independent | **Accepted, all corrected.** Effective independent CS-adjacent coverage in the original search: one gate (G34-B3), not five. |
| The F6 search never checked the standard Einstein-Cartan odd-in-torsion invariants (Nieh-Yan, Holst) at all -- the first thing a literature search for "action term odd in torsion" should find | **Accepted as a real search-completeness gap**, not pursued further this round (see next steps). |
| **A high-confidence reconstruction of `a`/`b`, unprompted:** OB13 itself already names the ONLY object its own parity audit found to be genuinely odd -- "E2's Dirac family `σ(n+3/2)+(t-1/2)h_H`", with `h_H=3` (round67). Setting it to zero: `t=1/2-σ(n+3/2)/h_H`; roots at `{0,1}` <=> `h_H=2·σ(n+3/2)` <=> `\|a\|=2\|b\|` with `a=h_H=3`, `b=σ(3/2)=3/2` at `n=0`. Cross-confirmed independently against C107's own finding of "two branches `3t` and `3t-3`" -- exactly `σ(3/2)+3(t-1/2)` rearranged | **Accepted as the most likely real referent, recorded as `[HYPOTHESIS]`** -- if correct, `I9`/`\|a\|=2\|b\|` is NOT a volume-based candidate at all; it is the ALREADY-ASSESSED E2 Dirac family, which `PARENT_ACTION_GATE.md` F4's own already-tried list logs as **round116: "equivalent restatement... no new content"** -- a DIFFERENT prior round than the one originally cited (round115). Duplicate match corrected below. |
| **A genuinely new, unexplored, well-motivated candidate this round's own kill criterion was looking for:** `η(D^t)`, the APS eta-invariant / gravitational Chern-Simons level, already partially computed in this project (`k_grav=η(D^{S³})/2=0`, G34-B3) -- but ONLY at the Levi-Civita point `t=1/2`. By C44's own already-certified identity `spec(D^{1-t})=-spec(D^t)`, `η(D^t)` is odd in `(t-1/2)` STRUCTURALLY (not by hand-construction, unlike I9), and it is scale/radius-INDEPENDENT by definition (an index-theory invariant, not a volume), so round115's circularity mechanism cannot arise for it at all. Its INTEGRALITY (a standard APS-theory question) would be a genuine, non-circular, action-level (gravitational Chern-Simons) selection condition | **Accepted as this round's most valuable actual output.** Not computed this round (was not the object under test) -- recorded as a new pearl below, the correct target for a future round if OB1's F4 is revisited. |

## Corrected verdict on the literal `I9` (volume-product reading)

Under this project's own already-established constraint surface
(G51+G57), `I9 ∝ (2t-1)·Vol(S³)·Vol(S⁶)` reduces to a function of `t`
alone: **linear**, zero only at `t=1/2` (wrong value -- KT-8 already
showed this point has no zero mode), unbounded if read as an
extremization target. This -- not "circularity" -- is the correct,
radius-independent kill for the LITERAL volume-product formula, if
that is genuinely what was meant.

## Corrected verdict on the likely REAL referent (E2 Dirac family)

If `a`,`b` are `h_H=3` and `σ(3/2)=3/2` as reconstructed above, `I9`
is not new: it is the E2 Dirac family `σ(n+3/2)+(t-1/2)h_H`, already
assessed in `PARENT_ACTION_GATE.md` F4's own already-tried list under
**round116**: "equivalent restatement of `D^t` being affine with
scalar slope, not new information" -- plus round116's own separately-
flagged multiplicity gap (C64: crossing multiplicities `2,2,6,6,12,12`,
none equal to 1). The duplicate match is corrected from round115 to
round116.

## New pearl (this round's actual most valuable output)

`η(D^t)` at general `t` (not just `t=1/2`) is unattempted, structurally
odd (no hand-construction needed), radius-independent (immune to
round115's circularity by construction), and would be a genuine
gravitational-Chern-Simons-level integrality condition if computed --
recorded in `pearl_registry/INDEX.md`.

## What this round does NOT show

- Does not prove `I9`/the reconstructed E2-family reading is
  definitively dead as a NEW argument -- round116's own "equivalent
  restatement" verdict stands as cited, not re-litigated here.
- Does not compute `η(D^t)` at general `t` -- named as the next step,
  not attempted.
- Does not confirm the `a=3,b=3/2` reconstruction is what the external
  session actually meant -- `[HYPOTHESIS]`, the most likely reading
  given this project's own record, not verified against the external
  source itself.
- Does not change N_gen=3's CONDITIONAL status, OB1's PARKED status
  (a gate-fill check is not a gate decision), or touch S6/triality.
- Does not solicit Tom Lawrence's Part 5.

## Kill Analysis

**What was killed:** the ORIGINAL argument for why `I9` fails (round115
circularity via 3 free parameters) -- wrong, corrected.

**What was NOT killed, and sharpened:** the top-line conclusion that
`I9` as a literal volume-product does not clear the gate -- survives,
now via a linear-runaway/wrong-zero argument instead of circularity.

**What this round adds, not present before:** (1) a likely real
referent for the previously-opaque `a`/`b`, redirecting the duplicate
match to round116; (2) a genuinely new, well-motivated, unexplored
F6-shaped candidate (`η(D^t)` at general `t`) that this round's own
search was supposed to find and initially missed.

## Verification

- No code was written or run this round (pure gate-fill/literature
  comparison, both before and after correction) -- no pytest re-run
  needed, consistent with this project's own convention for docs-only
  rounds.
- Skeptic independently re-derived the G51/G54A/G57 constraint chain,
  the `Vol(S³)·Vol(S⁶)=Vol(S³×S⁶)` identity, the `a=h_H=3,b=3/2`
  reconstruction (cross-checked against C107's independently-stated
  "3t and 3t-3" branches), and the `η(D^t)` oddness argument (from
  C44's already-certified spectral identity) -- all from this
  project's own already-committed files, not asserted from outside.
