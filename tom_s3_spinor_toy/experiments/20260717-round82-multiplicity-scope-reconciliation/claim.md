# claim.md — Round 82: Does the E12/E13 (KT-13) multiplicity excess (6 vs 3)
threaten the published N_gen=3 headline (G73/G74A/G74B), or is it internal to
a separate, already-non-load-bearing exploratory line (the S³-torsion
escape route, E1-E15 / KT-8 through KT-13)?

**Stakes:** internal-only (reading and reconciling existing, already-written
decision.md / preprint.tex / README.md / report text — no new computation,
no external publication action taken here).

**Question type:** descriptive (classifying an existing logical relationship
between two already-completed research lines), not causal or predictive.

## Zero-Signal Gate

- **Entity:** two specific things — (a) the published G73+G74A+G74B chain's
  claim that `dim ker(D_{S⁶}⊗S⁻) = 1` exactly per triality channel, and
  (b) the E12/round78 finding that `dim ker(D_{S³,t=0 or 1}) = 2`, giving 6
  total internal zero modes across 3 channels, not 3.
- **Falsifiable predicate:** either (a) and (b) refer to, and combine into,
  the SAME operator whose kernel dimension the headline N_gen=3 claim
  actually depends on — in which case the excess is a live threat — or they
  do not, in which case the excess is scoped to a separate line of work.
- **Measurable outcome:** read the exact operator definitions in G74A's and
  E12's own decision.md files, and read E7/E12/KT-8/KT-13's own stated
  relationship to G73/G74A/G74B, plus preprint.tex/README.md's own current
  wording — a definite verdict is derivable from these citations alone.

## Pre-registered possible verdicts

1. **THREATENS_HEADLINE** — the S³-side kernel (dim=2) is, or has become,
   part of the operator whose kernel-dimension the published N_gen=3 PROMOTE
   verdict (G73/G74A/G74B) actually certifies; the multiplicity excess is a
   live defect in an already-promoted, publication-facing claim.
2. **ORTHOGONAL_EXPLORATORY_LINE** — G73/G74A/G74B's `dim ker=1` result is
   about the S⁶-factor operator `D_{S⁶}⊗S⁻` alone, with no S³ factor
   entering it at all; the S³-torsion program (E1-E15) is a logically
   separate attempt to patch a *different*, later-discovered gap (KT-8: the
   untwisted full 9D product operator has zero kernel) that was never part
   of the G73/G74A/G74B derivation or PROMOTE verdict; the multiplicity
   excess found in E12/E13 is internal to that separate line and does not
   change G73/G74A/G74B's status.
3. **AMBIGUOUS_NEEDS_TEXT_FIX** — the mathematical relationship is
   orthogonal (as in #2), but `preprint.tex`, `README.md`, or
   `RESEARCH_STATUS_REPORT.md` currently state or imply (even by omission or
   loose phrasing) that the torsion route IS necessary for, or already
   folded into, N_gen=3 — creating a real overclaim/inconsistency in the
   public-facing claim surface that needs a scope-language fix.

## What this does NOT mean

1. This reconciliation does NOT re-derive or re-verify G73/G74A/G74B's own
   math, or E12's own math — both are taken as already tool-verified in
   their own decision.md files and re-cited here, not recomputed.
2. Does NOT resolve H1c/H2/H3 (physical selection of t=0 vs t=1) or the
   6-vs-3 multiplicity gap itself — that is out of scope; this experiment
   only classifies which claim surface the gap actually threatens.
3. Does NOT imply that the S³-torsion program was worthless or should stop
   — a verdict of ORTHOGONAL_EXPLORATORY_LINE says the STAKES of leaving the
   multiplicity gap unresolved are lower than E12's own framing might
   suggest, not that the line of inquiry is uninteresting.

## Kill criterion

If a single instance is found in `preprint.tex`, `README.md`, or
`RESEARCH_STATUS_REPORT.md` where the torsion-escape-route (t=0/1) result is
cited as an already-accepted or necessary step of the N_gen=3 derivation
(rather than as an explicitly-flagged, physically-unmotivated candidate
mechanism for a *different*, later gap), verdict #2 is killed in favor of
#1 or #3.
