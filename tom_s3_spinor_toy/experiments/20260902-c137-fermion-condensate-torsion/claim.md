# C137 claim -- can a genuinely DERIVED (not postulated) fermion condensate
# source the S3 torsion, with a 4D-pseudoscalar structure that survives
# C134's chirality-flip kill and avoids fine-tuning? (C132's candidate P3,
# bar raised twice: pearl_registry row 32's original bar, and C134's own
# three-part addendum)

## Mode declaration

**Convergent-mode round.** Tests ONE specific, pre-registered claim to
completion. This is the last of C132's originally-generated candidate
list to be tested today (P0->C133 confirmed/narrowed, P2->C134 killed,
P14->C136 killed-as-restatement; P3 is explicitly what C134 itself
"redirects into" after killing P2).

## Question type (EstimandOps L0)

**Descriptive** -- existence of a dynamically-derived condensate
solution, not merely a postulated one. Explicitly NOT causal, NOT
predictive.

## The bar this round must clear, stated precisely (do not lower it)

This candidate has TWO separate, independently-imposed acceptance
bars, both of which must be addressed -- read the sources directly,
do not paraphrase from memory:

1. **`pearl_registry/INDEX.md` row 32** (find it, quote it exactly) --
   names the fermion-condensate mechanism as "a genuinely third
   mechanism-type" distinct from symmetry-based and topology-based
   selection, and sets its acceptance bar as: check for an ANALOGOUS
   DYNAMICAL DERIVATION of WHY that direction is selected, not just an
   assertion that a condensate exists. A postulated, undirected
   condensate does not clear this bar.
2. **`experiments/20260902-c134-ecsk-torsion-auxiliary/decision.md`**
   Section 8, "What this round does NOT kill" -- read this in full.
   C134 raises P3's bar with three specific, named requirements: (a)
   the condensate must be a genuine **4D pseudoscalar** (not a vector,
   not a scalar -- C134's route 2 kill mechanism specifically targets
   axial/pseudoscalar-type bilinears that flip 4D chirality; a
   different Lorentz structure might escape it, but must be shown to,
   not assumed to); (b) it must be **reconciled with 4D chirality**
   (C134's route 2: this project's own certified zero modes are
   4D-chirality-DEFINITE per L5/G74B -- any condensate mechanism must
   explain how a nonzero condensate coexists with, or is compatible
   with, chirality-definite content, given C134's exact operator
   identity `P_L Γ⁰Ω₃P_L=0`); (c) it must **explain the fine-tuning**
   C134's route 3 found (`2t-1 = (kappa_13/4)*J` varies continuously
   with the condensate's magnitude, so `t in {0,1}` requires
   `|kappa_13*J|=4` exactly at unit radius -- a postulated condensate
   with no independent reason to have that specific magnitude is
   fine-tuning, not selection).

## Background, stated honestly before any computation

Read, in full, before doing anything else:
- `pearl_registry/INDEX.md` row 32 (quote exactly).
- `experiments/20260902-c132-13d-parent-action-survey/decision.md`'s
  `P3` entry (search "### `P3`") -- the literature already retrieved
  there: Manousselis-Prezas-Zoupanos arXiv:hep-th/0511122 (nearly-
  Kähler coset spaces, gaugino+dilatino condensates -- established on
  this project's own class of `S6`); Gemmer-Lechtenfeld arXiv:1308.1955;
  Cardoso-Curio-Dall'Agata-Lüst hep-th/0310021; Frey-Lippert
  hep-th/0507202. C132's own Mechanism-Transfer-Gate caution: these are
  10D HETEROTIC constructions, and rounds 87/88 already killed two
  earlier string-theory imports precisely because the mechanism did not
  transfer -- the transferable OBJECT is the algebraic relation
  "fermion bilinear sources totally-antisymmetric torsion" (already
  established generically, not string-specific), NOT the heterotic
  packaging (gauge sector, specific condensation dynamics, SUSY
  structure). State explicitly, for each source you use, whether you
  are importing the transferable algebraic relation only, or something
  more specific that needs its own justification.
- `experiments/20260902-c134-ecsk-torsion-auxiliary/decision.md` IN
  FULL, especially Section 5b (the three kill routes) and Section 8
  (the redirect-into-P3 language and its three-part bar).
- `experiments/20260901-c125-full-gauge-equivalence-gate/decision.md`
  Section 0a/2a for the torsion normalization `T^t=2(2t-1)eps` this
  round must match.
- `null_results/INDEX.md` entries for round87, round88 (the killed
  string-theory imports -- read WHY they failed, so this round does not
  repeat the same transfer error under a different citation).
- `AOG-5` (Anti-Overfitting Gate, independent motivation criterion) in
  `~/.claude/rules/falsification-ladder.md` if accessible, or apply its
  substance directly: is there an INDEPENDENT reason (not just "this
  would rescue the candidate") to expect this specific condensate to
  exist and have this specific magnitude?

## The Zero-Signal Gate check, required before proceeding

Per `falsification-ladder.md` Step -5: `(exists entity) AND (exists
falsifiable predicate) AND (exists measurable outcome)`, all three
required.

- **Entity:** a specific fermion bilinear condensate `<chibar Gamma_abc
  chi>` (or the appropriate 4D-pseudoscalar analogue per C134's
  requirement (a)) on this project's frozen `S3xS6` background, with an
  explicit statement of WHAT FIELD `chi` is (is it the same zero-mode
  content L5/G74B already certifies, or a genuinely different,
  independently-motivated field this project does not yet have?).
- **Falsifiable predicate:** this condensate can be shown to arise from
  an actual dynamical mechanism on this background (a derived vacuum
  condition, a minimization argument, a symmetry-breaking pattern
  specific to this geometry) -- NOT simply postulated to exist because
  it would rescue `t` selection.
- **Measurable outcome:** either (a) an explicit dynamical derivation
  showing the condensate's existence and approximate magnitude follow
  from this project's own frozen content, or (b) a clear, honest
  statement that no such derivation exists and the condensate would
  have to be postulated as new, unmotivated physics.

**If no dynamical derivation can be found or constructed, and the
condensate would have to be a bare postulate, this round should report
`REJECT` (fails pearl_registry row 32's bar) or `BLOCKED` -- NOT force
a positive result by quietly treating "a condensate that would work"
as if it were "a condensate that is derived". This is explicitly
permitted and is not a failure of the round.**

## Falsifiable claim

A 4D-pseudoscalar fermion condensate, dynamically motivated (not merely
postulated) by this project's own frozen `S3xS6` geometric content,
sources the `S3` torsion with a magnitude landing at `t in {0,1}`
without fine-tuning, and is compatible with (does not contradict) the
4D-chirality-definite zero-mode content L5/G74B already certifies.

## Kill criterion

FALSE if: (a) no dynamical motivation for the condensate's existence
can be constructed beyond "it would rescue t-selection" (fails AOG-5 /
pearl_registry row 32's bar -- report as REJECT, this is the single
most likely outcome given the literature review already performed in
C132); (b) the condensate, if motivated, is not a 4D pseudoscalar (a
different Lorentz structure escapes C134's specific kill mechanism but
must independently justify its own existence and magnitude); (c) the
condensate's required magnitude for `t in {0,1}` has no independent
justification (fine-tuning persists); (d) the condensate genuinely
contradicts the certified chirality-definite content (a real physical
inconsistency, not just an unmotivated addition).

## What this round does NOT show

- Does NOT claim any positive result establishes new physics beyond
  this project's own frozen content without saying so explicitly.
- Does NOT reopen C123-C136's verdicts.
- Does NOT change `N_gen=3`'s CONDITIONAL status, `lambda=
  FREE_COUPLING_PARAMETER`, or `safe_for_runtime=False`.
- Does NOT close H1c, OB1, or round95's gap even if it somehow
  succeeds -- it would supply one candidate mechanism among many still
  needed.
- Does NOT solicit Tom Lawrence's Part 5.

## Verification plan

- Read all cited files in full before any computation, especially
  round87/round88's actual failure reasons (to avoid repeating the
  transfer error) and C134's exact three-part bar.
- Literature-first: if additional sources beyond C132's already-
  retrieved four are needed to assess dynamical motivation, retrieve
  and read them this session (do not rely on memory).
- Be honest and specific about the Mechanism-Transfer-Gate distinction
  (transferable algebraic relation vs. non-transferable heterotic
  packaging) for every source cited.
- Cite `[VERIFIED]`/`[CITED]`/`[INFERRED]`/`[SPECULATIVE]` throughout.
- FL Step 8a skeptic pass (context-blind: only claim.md + decision.md +
  code, no session history). Given this round's likely outcome is a
  REJECT on dynamical-motivation grounds (a largely literature/argument-
  based round, not a heavy computation), a single pass suffices unless
  it finds something that changes the verdict's direction, in which
  case run a second, differently-worded pass (Paraphrase-Sensitivity
  Probe).
