# G90 Decision — UV Completion Requirements and No-Go Map

**Verdict:** `NO_GO_MAP_COMPLETE`

## Summary

This gate does not derive new physics. It consolidates the current state of the
repository into a conservative external-facing package:

- the lambda origin route is exhausted inside the current geometric/spectral
  framework;
- the physical mass ratio is still proxy-level and requires a full reduced 4D
  action;
- the right-handed neutrino sector is Dirac-only in the current branch unless
  external `B-L` breaking physics is added.

## What the repository supports

- a phenomenological spectral compactification toy model;
- exact one-generation and three-generation geometric structure;
- a clear `FREE_COUPLING_PARAMETER` status for `lambda`;
- a clear Dirac-only neutrino status in the current branch.

## What remains open

- a UV-complete origin for `exp(-lambda/rho6^2)`;
- a full reduced 4D action and canonical radion normalization;
- a same-frame KK scale map;
- an explicit `B-L = +2` sector if Majorana or seesaw neutrinos are desired.

## Package outcome

This is now a falsification-first boundary map. It is suitable for sharing with
external physicists because it states what the repository can defend and what it
cannot.
