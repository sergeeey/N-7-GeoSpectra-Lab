# C132 claim -- DIVERGENT-MODE survey: what candidate 13D parent-action
# mechanisms could supply the missing S6-triality-channel <-> S3-t-sector
# pairing rule (H1c/OB1's central open question)?

## Mode declaration (per `research-methodology.md`'s Dual-Loop Engine)

**This is explicitly a DIVERGENT-circuit round (D1-D5), NOT a convergent
one.** Every prior round today (C123-C131) was convergent: given one named
candidate, attack it, verify or kill it. This round is different in kind:
the user explicitly chose, after being told the honest cost/risk/
uncertainty, to attempt DISCOVERY of new candidate constructions, not
verification of an existing one. Do not collapse this into a single
narrow claim.md-style falsifiable predicate about ONE mechanism -- that
is the anti-pattern this file's own methodology names ("Скепсис на
стадии scouting" / skepticism at the scouting stage, over-killing before
raising). At this stage a candidate marked `[SPECULATIVE]`/`[CANDIDATE]`
is a legitimate output; promotion to a full FL claim is NOT this round's
job.

**Skeptic-leaning tie-breaker override, stated explicitly:** the
project's own default (skeptic-leaning when stage is unclear) does NOT
apply here, because the stage is NOT unclear -- it is explicitly
scouting, by direct user instruction. Do not kill a `[SPECULATIVE]`
candidate for being under-verified; that is the expected, correct state
of a scouting-stage output.

## Why this question, stated honestly

Today's entire C123-C131 chain (bordism/anomaly arguments, gauge-group
reconciliation, Pin structures, twisted Pin structures, dimensional
consistency) converged, independently, four separate times, on the same
single remaining blocker: **H1c** (also called "round95's missing S6-S3
link" throughout those files) -- the absence, anywhere in this project's
frozen content, of an established rule linking "which S6 triality-channel
(of the 3 that produce the 3 generations, per G73/G74A/G74B)" to "which
S3 connection-family t-sector (t=0 or t=1)". Round95
(`experiments/20260717-round95-l5-tension-check/decision.md`, 2026-07-17,
already complete, do not re-run) diagnosed this precisely and stated what
closing it would require: *"A stated 13D parent action specifying
independent fields for t=0 and t=1 and how each couples to the 3
triality channels -- not attempted here."*

C124 (`experiments/20260901-c124-parent-invariant-classification-
preregistration/decision.md`, today) already exhaustively classified ONE
specific class of candidate 13D parent actions -- local, polynomial,
first-order, bosonic Lovelock-Cartan invariants on the strict product
background, no independent 13D gauge field -- and found `STRUCTURAL_NO_GO`
for that entire class. **This round's job is to survey what lies OUTSIDE
that already-closed class**, per C124's own explicitly-stated Relaxation
Map (`V1`: an independent 13D gauge field; `V2`: non-polynomial functions;
`V3`: additional p-form fields; `V4`: an enlarged gauge algebra beyond
`SO(1,12)`; `V5`: boundary/defect terms -- V5 alone was separately
checked and also closed; `V1-V4` remain genuinely open) and OUTSIDE the
strict-product/bosonic-only restriction (non-product/warped backgrounds,
fermion bilinears).

## EstimandOps L0, at the survey level

**Descriptive/existence, explicitly at a SURVEY granularity, not a single
falsifiable claim.** The question this round answers is: "what are the
serious, nameable candidate mechanisms (not yet ruled out by this
project's own registries) for a 13D parent action supplying the
S6-channel <-> S3-t-sector pairing, and which are cheapest to test
first?" -- NOT "is mechanism X correct?" for any single X. Each
individual candidate this round surfaces gets, at most, a cheap
plausibility screen (see Step 4 below), not a full derivation.

## Step 1 -- MANDATORY Novelty Check FIRST (FL Step -3), before generating anything

Before proposing a single new candidate, grep and read:
- `null_results/INDEX.md` in full -- multiple prior rounds already killed
  specific 13D-parent-action-adjacent candidates (search for "parent
  action", "13D", "G97", "Pati-Salam", "gauge completeness", "CSDR").
- `parked/INDEX.md` if it exists.
- `pearl_registry/INDEX.md` in full -- several entries this session alone
  are directly relevant (the C125-vs-C126 gauge-group reading conflict;
  C127/C128/C129/C130/C131's whole chain on what mechanisms can and
  cannot discriminate `t`).
- `PARENT_ACTION_GATE.md` in full, especially field F1 (background/twist
  content), F2 (gauge content), and F4's entire accumulated history
  (every mechanism tried and why it failed, C118 through today).
- `OPEN_BLOCKERS.md` OB1 (KT-8), OB13 (H1c/C25 parity), OB11 (matter-
  generation factorization) in full.
- The sibling-project external survey already cross-referenced in OB1
  ("Checked, external survey, no computation in N-7 itself (sibling
  project H-19 'Геометрия калибровки', 2026-08-31... a targeted
  literature survey of 6 known geometric-gauge/parameter-selection
  schemes... None of the 6 transfers") -- read that survey's actual
  content if the referenced external file is reachable, since it may
  contain candidates relevant to THIS narrower question (a parent action
  supplying a channel<->sector pairing) even though it was run for a
  different specific question.
- `experiments/20260717-round86-parent-action-discriminator/decision.md`
  and the surrounding round86-89 chain (external string-theory analogies
  already tried and found not to transfer).
- Any existing "100 directions" or brainstorm-type document in this
  project (search for it) -- there is project memory of a prior
  "100 Directions Brainstorm" round; check whether it already covers this
  specific angle before re-generating from scratch.

**Kill signal for any candidate this round would otherwise propose:** if
it matches (not just superficially, but by MECHANISM -- per this
project's own Mechanism Transfer Gate, 6-field check: FORMULA/OBJECT/
ROLE/GENERATOR/SECTOR/OBSERVABLE) something already in `null_results/` or
already explicitly ruled out in `PARENT_ACTION_GATE.md` F1/F2/F4, do not
re-propose it as new -- cite the prior kill and explain what, if
anything, is genuinely different about the new angle.

## Step 2 -- generate the candidate space (divergent)

Aim for genuine breadth, not depth. For each candidate, name:
- **Mechanism**: what physical/geometric principle supplies the pairing.
- **What new ingredient it requires** relative to this project's frozen
  content (an independent gauge field? non-product background? fermion
  bilinears? something else?) -- and whether that ingredient is itself
  already flagged as a "granted hypothetical" elsewhere in this project
  (e.g. C124's V1, C127's Relaxation Map item X5, C130's W2/W3).
- **Where a similar mechanism has worked in the broader literature**
  (Kaluza-Klein model-building, coset-space dimensional reduction,
  flux compactification, spectral-triple/NCG constructions -- this
  project's own OB2 track is directly relevant here and likely
  under-explored for this specific question, spectral-triple/NCG
  literature-search first per this project's own standing practice of
  retrieving and reading primaries, not trusting memory).
- **Whether this project's own frozen content is even compatible with
  it** -- a fast, cheap check, not a full derivation.

Consider explicitly, at minimum (do not treat this as exhaustive --
generate beyond it):
- C124's own named V1 (independent 13D gauge field) -- what is the
  cheapest, most natural candidate FOR such a field given this project's
  actual G2/SU(3) structure group content already established (G73,
  G74A/B, G108)?
- Non-product/warped background corrections to the strict product ansatz
  (C124's own explicitly-named escape) -- is there a natural, minimal
  warping ansatz consistent with this project's already-fixed radii
  (rho_3, rho_6) and flux data?
- Coset Space Dimensional Reduction (CSDR) -- already flagged in OB1 as
  "a kindred stuck problem, not a solution: 45 years of literature never
  selected the isotropy embedding either" -- is there a NARROWER CSDR
  sub-question (not "select the isotropy embedding" but specifically
  "pair S6 triality channels with S3 t-sectors") that the broader
  CSDR-failure verdict does not actually cover?
- Spectral-triple / Connes-style NCG parent action (this project's own
  OB2 track, round103/110's partial toy attempts) -- does a genuine NCG
  spectral action naturally produce a channel<->sector pairing as part of
  its own structure (e.g. via the Dirac operator's own block structure).
- Fermion-bilinear terms in the parent action (C124's own explicitly
  UN-closed gap) -- a Yukawa-type or mass-mixing term coupling S6-sector
  and S3-sector fermion content directly.

## Step 3 -- cheap screen, not full verification

For each surviving candidate (post-Novelty-Check), apply ONLY the
Zero-Signal Gate (entity/predicate/measurable-outcome nameable from this
project's frozen content or from an explicitly-granted, clearly-labeled
hypothetical extension) -- do NOT attempt a full derivation or a skeptic
pass on every candidate. This is a screen, not a verdict.

## Step 4 -- rank by Cheapest Differentiating Test value

Per this project's own CDT Protocol
(`max(differentiation + kill_power + rescue_power + reuse_value -
circularity_risk) / cost`), rank the surviving candidates. Output a
prioritized Relaxation Map (NOT a single winner) -- this round's final
deliverable is the ranked list plus, for the TOP 1-2 candidates only, a
properly pre-registered claim.md-style spec ready for a FUTURE convergent
round to execute (do not execute it in this round).

## What this round does NOT do

- Does NOT derive or verify any specific 13D parent action to completion.
- Does NOT claim any candidate is correct -- `[SPECULATIVE]`/`[CANDIDATE]`
  status only, explicitly permitted per the mode declaration above.
- Does NOT reopen or re-litigate C123-C131's verdicts.
- Does NOT change `N_gen=3`'s CONDITIONAL status, `lambda=
  FREE_COUPLING_PARAMETER`, or `safe_for_runtime=False`.
- Does NOT solicit Tom Lawrence's Part 5.
- Does NOT run a full FL Step 8a skeptic pass on every candidate (that
  would defeat the purpose of a cheap screen) -- but DOES run one on this
  round's OWN Novelty Check (did it actually find everything relevant?)
  and on the final ranked list's reasoning, since a bad ranking wastes a
  future round's time.

## Deliverable format

A `decision.md` in this project's house style, but shaped for a
divergent-mode round: Novelty Check results first (what's already ruled
out, cited precisely); the generated candidate space (aim for at least
8-15 genuinely distinct candidates, not padding -- quality over quantity,
but breadth is the point); each candidate's Zero-Signal Gate screen
result; a CDT-ranked priority list; and, for the top 1-2, a ready-to-run
claim.md sketch for a future convergent round. End with an FL Step 8a
skeptic pass on the Novelty Check's completeness and the ranking's
reasoning (not on every individual candidate).
