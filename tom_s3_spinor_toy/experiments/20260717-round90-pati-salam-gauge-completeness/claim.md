# E21 (round90) — Claim: does this project's own commitment to gauging
# `SU(2)_R`, combined with standard Pati-Salam fermion-completeness
# model-building, supply a genuine, spacetime-native (not string-worldsheet-
# borrowed) reason requiring the `t=0` and `t=1` torsion-crossing sectors to
# coexist?

**Date:** 2026-07-17
**FL tier:** [x] Full (research claim, per project CLAUDE.md methodology
activation; this is a fourth, independent candidate for E18/round86's parent-
action/coexistence gap, following three failed string-worldsheet-borrowed
attempts in round87-89)
**Question type:** [x] descriptive [ ] predictive [ ] causal

## Estimand (L0/L1, descriptive)

**Population:** (a) this project's own `preprint.tex` text, specifically every
passage committing to (or merely labeling) `SU(2)_R`; (b) the external,
standard Pati-Salam / left-right-symmetric-model literature's stated
requirement on fermion representation content under a gauged
`SU(4)×SU(2)_L×SU(2)_R` (or `SU(3)_c×SU(2)_L×SU(2)_R`) symmetry.

**Intervention/comparator:** none — this is a classification task: read this
project's own text precisely enough to determine whether it commits to
`SU(2)_R` as a genuine 4D gauge symmetry (gauge boson, entering the
KK-reduced/spectral action) or only as a bookkeeping label (`T3R` quantum
number with no claimed gauge boson); separately, ground the external
Pati-Salam requirement in a primary/standard source; then check whether the
project's specific commitment level actually triggers that requirement, and
whether doing so closes E18/E19/E20's open parent-action/coexistence gap
without introducing a new, unresolved gap of its own.

**Endpoint:** a three-way categorical verdict (PASS / FAIL / BLOCKED, exact
frozen definitions below).

**Summary measure:** categorical (PASS/FAIL/BLOCKED), not continuous.

**ICE:** none — closed-form classification against a finite, already-existing
text plus a small set of external, independently-verifiable sources.

**MCID:** not applicable to a categorical verdict.

## Natural-language statement (written before re-deriving the verdict)

*We classify, descriptively, whether `preprint.tex`'s own stated commitment to
`SU(2)_R` — genuinely gauged (with a real 4D gauge boson, entering the
spectral action's gauge kinetic term and the Higgs bidoublet's representation
assignment) versus merely a bookkeeping label (`T3R` used only to assign
quantum numbers) — combined with the external, standard Pati-Salam/left-right
fermion-completeness requirement (grounded in Pati-Salam 1974 and/or a
standard modern review, and in the well-established Witten `SU(2)` global
anomaly, Phys. Lett. B117 (1982) 324), together supply a genuine,
non-ad-hoc, spacetime-native physical reason requiring both the `t=0` and
`t=1` torsion-crossing sectors of the `S³` connection family to be physically
present (PASS); or whether `SU(2)_R`'s appearance in this project's text stays
at a bookkeeping-label level that never triggers this requirement (FAIL); or
whether the gauging commitment itself is ambiguous, or the full
`SU(4)×SU(2)_L×SU(2)_R` combination this argument would need is itself
admitted incomplete/open in this project's own text (BLOCKED).*

## What this result does NOT mean (written before re-deriving the verdict)

1. Will **not** establish H1c (a physical mechanism selecting `t=0`, `t=1`,
   both, or neither as THE realized connection) — even a PASS here would only
   supply a reason both representations' worth of matter must exist in a
   complete theory, not a mechanism fixing which value(s) of the continuous
   parameter `t` a physical compactification actually has.
2. Will **not** resolve KT-8 (whether ANY zero mode of the full, untwisted 9D
   operator `D_full` exists) — untouched regardless of this experiment's
   verdict; the torsion-escape-route program remains, per `preprint.tex`'s own
   words, "a candidate mechanism... physically unmotivated, not a resolution"
   (`preprint.tex:1467-1497`, reused via E18-E20).
3. A PASS verdict, if reached, would **not** by itself supply the missing
   parent action (E18/KT-1's core gap: a stated 13D Lagrangian with
   independent fields for `t=0` and `t=1` and their own equations of motion) —
   a representation-theoretic consistency ARGUMENT (why the content must
   exist) is not the same object as a stated ACTION (how the content
   dynamically arises). This distinction is checked explicitly in Section 5 of
   `decision.md`, not glossed over.
4. A BLOCKED or FAIL verdict, if reached, would **not** mean this project's
   `SU(2)_L×SU(2)_R` gauge-structure claim is wrong or should be retracted —
   only that the SPECIFIC question of whether it forces `t=0`/`t=1`
   coexistence is not decidable, or not triggered, from what this project's
   text currently states.
5. Will **not** re-derive or challenge E9-E20's own already tool-verified
   results — reused here by citation. Will **not** re-litigate round87-89's
   already-closed string-worldsheet-borrowed candidates (Gates-Hull-Roček,
   classical Strominger-Hull, base `(1,1)` sigma model, WZW unitarity) — this
   experiment's candidate is explicitly a DIFFERENT, spacetime-native
   argument (4D gauge-theory consistency, not 2D worldsheet chirality).
6. Does **not** claim this project's own explicit anomaly-cancellation
   computation (`preprint.tex:309-320`) already checks the specific
   `SU(2)_R`-gauged consistency condition this experiment investigates — this
   is checked directly, not assumed either way, in `decision.md`.

## Pre-registered PASS / FAIL / BLOCKED criteria (frozen verbatim, per task instructions)

| Verdict | Condition |
|---|---|
| **PASS** | The project genuinely commits to gauging `SU(2)_R` (a real 4D gauge boson, not merely a label), AND standard Pati-Salam/left-right fermion-completeness genuinely requires both `t=0`/`t=1` content given that commitment, AND no further gap remains in transferring this requirement to this project's specific construction. |
| **FAIL** | `SU(2)_R` stays label-only in this project's own text (used only to assign quantum numbers `T3R`, with no claim of an actual gauge boson) — it therefore never triggers any Pati-Salam-style completeness requirement. |
| **BLOCKED** | The gauging commitment is ambiguous, OR the full `SU(4)×SU(2)_L×SU(2)_R` combination this argument would need is itself admitted incomplete/open in this project's own text, per an honest reading. |

## Method

1. Grep and directly read `preprint.tex` for every mention of "Pati", `SU(2)_R`,
   `SU(4)`, "gauge group", "gauge boson", "gauge field", "spin connection",
   "gauge kinetic". Determine precisely, with exact quotes and line numbers,
   whether the text ever constructs or claims an actual `SU(2)_R` GAUGE
   FIELD/BOSON entering the 4D effective action (e.g., via the standard
   Kaluza-Klein spin-connection-as-gauge-potential mechanism this project
   already uses for `SU(2)_L` and `SU(3)_c`, or via the spectral action's
   gauge kinetic term), or whether it only ever uses `SU(2)_R`-type labels
   (`T3R`) as bookkeeping.
2. Ground the external Pati-Salam fermion-completeness requirement in an
   actual source: WebSearch/WebFetch Pati-Salam's original 1974 paper
   ("Lepton Number as the Fourth Color," *Phys. Rev. D* 10, 275) and/or a
   standard modern review (Wikipedia, a cited secondary paper), plus the
   well-established Witten `SU(2)` global anomaly (Witten, *Phys. Lett.*
   B117 (1982) 324) as the rigorous, independently-citable consistency fact
   underlying "why both representations are needed when `SU(2)_R` is
   gauged." Attempt to fetch the primary 1974 text directly (via
   `pdftotext`, matching this project's own round87-90 practice); if the
   extraction is too corrupted to quote reliably, say so honestly and rely
   on the converging secondary sources instead, marked at the appropriate
   confidence level.
3. Determine honestly whether this project's OWN specific commitment level
   (from step 1) actually triggers the requirement (from step 2), or whether
   it stays at a level that does not. Do not assume either direction in
   advance.
4. If the answer leans PASS: check further whether this argument resolves the
   SPECIFIC identification already established (does `t=0`'s content map to
   `(4̄,1,2)`/`SU(2)_R` doublet and `t=1`'s to `(4,2,1)`/`SU(2)_L` doublet,
   reusing E16/E17's already tool-verified representation labels without
   re-deriving them) — and whether a further gap remains: specifically,
   whether `SU(3)_c×U(1)_{B-L}` (from `S⁶`) combines with the claimed
   `SU(2)_R` gauging (from `S³`) into ONE single, geometrically well-defined
   `SU(4)×SU(2)_L×SU(2)_R` gauge group, or whether this project's own text
   flags this combination as itself incomplete/open (e.g.
   `preprint.tex:280-287`, `1586-1601`, gate G97 — already cited in round87).

## Kill criterion

If Step 1 finds `SU(2)_R` used ONLY as a quantum-number label anywhere it is
introduced (no claimed gauge boson, no gauge-kinetic-term entry, no Higgs
bidoublet assignment tied to it as a gauge symmetry) — this moves the verdict
to FAIL, cleanly, regardless of what Step 2 finds externally. If Step 1
instead finds a genuine gauging commitment, but Step 4 finds the
`SU(4)×SU(2)_L×SU(2)_R` combination is itself admitted incomplete/open in this
project's own text (not merely by this experiment's own inference) — this
moves the verdict to BLOCKED, not PASS, regardless of how solid the external
Pati-Salam grounding turns out to be. PASS requires both a genuine gauging
commitment AND a demonstrably complete, ungapped transfer — a high bar,
deliberately, given three prior candidates (round87-89) failed for
transfer-specific reasons after superficially promising starts.

## Assumptions (status)

| Assumption | Status |
|---|---|
| `E16`/`E17`'s representation labels (`t=0`↔`(1,2)`, `t=1`↔`(2,1)`, or their mirror under the unresolved `SU(2)_L`/`SU(2)_R` labeling convention, `CONVENTION_TABLE.md` row 6) | [VERIFIED-tool, inherited] — reused, not re-derived |
| `t=1`'s kernel exists only under `c0=-2` (`CONVENTION_TABLE.md` row 5) | [VERIFIED-tool, inherited] — reused, not re-derived |
| Pati-Salam's fermion-completeness requirement is standard, well-established GUT model-building, not this experiment's own invention | To be grounded externally in Step 2, not assumed |
| The Witten `SU(2)` global anomaly (odd number of `SU(2)` doublets ⟹ non-perturbative inconsistency) is a real, independently-citable QFT fact | To be grounded externally in Step 2, not assumed |
| Lemma L5's S⁶-side asymmetric chirality mechanism (`preprint.tex:884-908`, `sign(ind)=+1`) and E17 Section 5's flagged tension with it | [DOCS/VERIFIED-tool, inherited from E17] — reused, not re-derived or re-resolved here |

## Check

This is a literature-and-text classification round; there is no new
numerical script (per this project's own precedent, round86-90). The "check"
is: (1) every `preprint.tex` line cited in `decision.md` is read directly this
round, not from memory or a prior round's paraphrase; (2) every external
source cited is either directly fetched/extracted (`WebFetch`/`pdftotext`) or
explicitly marked `[WEAK]`/`[VERIFIED-tool]` per this project's own evidence
markers, matching round87-90's own sourcing discipline; (3) the final verdict
follows deductively from the pre-registered criteria table above applied to
the findings, with no step skipped or forced.
