# Round122 — Claim

**Gauge/Hilbert/Triality closure program, item 7 (global recomposition).**
Final item before item 8 (preprint rewrite, only after this verdict).

## L0 gate (EstimandOps)

**Question type: Descriptive.** Does the full 20-claim `CLAIM_LEDGER.yaml`
+ 3-chain `DERIVATION_GRAPH.yaml`, once everything through round121 is
accounted for, still cohere the way the headline claim (`N_gen=3`,
`CONDITIONAL`) currently states — or does recomposing the individually-
verified pieces silently smuggle in an untested assumption, per this
project's own `falsification-ladder.md` Recomposition Gate? Not a new
physics claim — an audit of whether the existing synthesis is still
accurate.

## Falsifiable claim

1. `DERIVATION_GRAPH.yaml`'s `D2_NGEN3_FULL_CHAIN` (premises: `C1`, `C2`,
   `C_G67C3`, `C3` as blocking defeater) remains a valid recomposition —
   i.e. the newest claims added this session (`C19_SPINOR_DECOMPOSITION_
   AUDIT`, `C20_MATTER_GENERATION_FACTORIZATION_THREE_WAY`) do NOT need to
   be added as additional premises or defeaters of `D2`, because they are
   parallel/explanatory investigations rather than premises the counting
   argument itself depends on.
2. `preprint.tex`'s own Open Problems L3b entry (lines ~1271-1296) is
   stale relative to this session's own `L3B_SPIN8_INTERFACE_SPEC.md` and
   round119's `TRIALITY_DISTINGUISHABILITY_GATE.md` — it does not mention
   the `SO(4)×SO(4)` partial advance, understating (not overclaiming) the
   project's own progress.

## Pre-registered check (before any interpretation)

1. Re-read `CLAIM_LEDGER.yaml` in full (all 20 entries) and
   `DERIVATION_GRAPH.yaml`'s 3 derivation chains.
2. For each of `C19` and `C20`, trace explicitly: does `D2`'s own counting
   argument (`ind=1` + `dim ker=1` + `sign(ind)=+1` + `3 channels`) actually
   USE anything `C19`/`C20` establish, or do `C19`/`C20` only test SEPARATE
   hypotheses proposed to EXPLAIN already-established facts (SU(4)-singlet,
   B-L=0) without being inputs to the counting itself?
3. Re-read `preprint.tex` lines 1256-1296 (full Open Problems L3a/L3b
   entries) and compare against `L3B_SPIN8_INTERFACE_SPEC.md`'s own later
   content and `TRIALITY_DISTINGUISHABILITY_GATE.md`'s verdict.
4. Do NOT edit `preprint.tex` this round — per this project's own
   established practice (round53's deferred editorial update, "one round,
   one deliverable"), produce a precise, actionable punch list for item 8
   (preprint rewrite) instead of applying edits pre-emptively.

## Kill criterion (pre-registered)

- If `C19` or `C20` is found to actually supply a premise `D2`'s counting
  argument depends on (not just an explanatory/parallel hypothesis) — `D2`
  must be updated to include it as a premise or defeater, and `C4`'s
  `depends_on` list in `CLAIM_LEDGER.yaml` is currently incomplete.
- If `preprint.tex`'s L3b text is found to already adequately reflect the
  `SO(4)×SO(4)` finding somewhere not yet grepped — the staleness claim is
  wrong, no punch-list item needed.
- If any OTHER claim in the 20-entry ledger is found inconsistent with
  `D2`'s stated status when checked directly (not assumed from its own
  `truth_status` field) — flag as a new, separate finding.

## What this does NOT mean (pre-registered)

1. Does NOT change `N_gen=3`'s `CONDITIONAL` status — this round checks
   whether that status is STILL accurate, not whether it should change.
2. Does NOT edit `preprint.tex` — produces a punch list for item 8 only.
3. Does NOT re-derive any of the 20 claims — reads and cross-checks their
   already-established status only.
4. Does NOT affect `lambda=FREE_COUPLING_PARAMETER` or
   `safe_for_runtime=False`.
