# C131 decision -- is `M_ι` the right DIMENSION, or did C130 compare it against
# a table that was never about it?  (C130's Relaxation Map item W1.)
#
# ⚠️ **THIS ROUND HELD THREE DIFFERENT CONCLUSIONS IN SEQUENCE.** Draft 1: "C130's
# flag stands, `M_ι` is EXCLUDED". Draft 2, after two context-blind FL Step 8a
# passes falsified draft 1: "C130's flag FALLS, three rounds are vindicated".
# Draft 3 (this file), after a third pass falsified draft 2: **C130's flag stands
# in SUBSTANCE, on a reason nobody had given -- neither C130, nor either draft,
# nor the first two passes.** All three drafts are quoted where withdrawn. The
# round's own history is the strongest evidence in it that the question was not
# obvious.
#
# HEADLINE:
#
# **C130's W1 flag is CORRECT about `M_ι`, WRONG in both premises it gave, and
# the real obstruction is ORIENTABILITY, not dimension.**
#
#   **The finding, and it was sitting in C129's own table for a day:**
#   `H₄(M_ι; ℤ) = 0` while `H₄(S³×S¹; ℤ) = ℤ`. `M_ι` is closed and
#   **non-orientable**, so it carries **no integral fundamental class** -- and
#   therefore `∫c₂`, *the very number C127 §5d computed*, **is not defined on
#   `M_ι` at all**. C129 printed both rows and its own skeptic pass called the
#   difference *"the orientability difference"*; nobody connected it to the
#   relevance question.
#
#   **Why that settles it:** three readings DO make a 4-manifold built from the
#   internal `S³` anomaly-relevant -- Chern-Simons level quantization, a `Q/Z`
#   coefficient on a coboundary, APS spectral flow (§6). **Every one of them
#   requires an orientable, indeed Spin, 4-manifold, and every one of them
#   produces C127 §5d's `S³×S¹` -- not `M_ι`.** `M_ι` is an element of none of
#   their bordism groups.
#
#   **So the split runs between the two OBJECTS, not between two conventions and
#   not between two layers:** C127 §5d's `S³×S¹` (orientable, spin, carries
#   `∫c₂ = −1`) has legitimate homes; `M_ι` (C128 §6b's substitute, non-orientable)
#   has none of them. **C128 §6's substitution is precisely the step at which the
#   object left every reading that could have justified it** -- and C128 made it
#   for a good reason (C128 §4: the map that actually relates `∇⁰` to `∇¹` is the
#   orientation-reversing `ι`, not a gauge transformation).
#
# **BOTH OF C130's STATED PREMISES ARE STILL FALSE, and that is not a quibble --
# it is why the flag was nearly overturned:**
#   · *"`Ω₄` appears in none of C127 §4's readings"* -- §4's list is
#     **non-exhaustive**: 4D topological terms need `Ω₄` and `Ω₅` and the row is
#     missing (§3b). That reading is separately closed (§6 S2), so the conclusion
#     survives, but the argument as given does not.
#   · *"this project has no 3D theory"* -- established by a grep for the **string**
#     `3d theory`, which cannot see `CS₃`. The project carries
#     `∫_{S³}CS₃(ω^t)` (C123) and **C127's own row X4 leaves the `S³`-internal CS
#     route explicitly OPEN** (§3c). What the project does *not* have is a 3D
#     **quantum field theory** -- `∫_{S³}CS₃(ω^t)` is a **number**, a coefficient
#     of a 4D term. Draft 2 inverted the round on that term shift; withdrawn.
#
# **CONSEQUENCE FOR C128-C130, stated without consolation:** their mathematics is
# untouched and C128 §6b is *correct* -- and being correct is exactly what costs
# the object its readings. C129/C130 established that `M_ι` admits `Pin`
# structures, which is the entry ticket to `Ω^{Pin}_4`; **that is C130's own
# objection restated, not answered.** Draft 2's *"three rounds were not wasted"*
# is **WITHDRAWN**: whether they bear on OB1 is `[UNKNOWN]`, and this round
# makes it look less likely, not more.
#
# NOT covered: any anomaly evaluation; round95; the group `G`; `N_gen=3`;
# Tom Lawrence's Part 5.

**Verdict (2026-09-02; THREE independent FL Step 8a skeptic passes RUN,
context-blind, differently-worded. Passes 1 and 2: `WEAKENED`, CONCORDANT, and
they falsified draft 1. Pass 3, on the rewritten file: `FALSIFIED`, and it
falsified draft 2. 45 findings; 41 accepted and repaired, 4 answered with
reason, 0 waved through. Matrices in §0):**
`C130s_W1_FLAG_IS_CORRECT_ABOUT_M_iota_BUT_BOTH_ITS_STATED_PREMISES_ARE_FALSE_AND_THE_REAL_OBSTRUCTION_IS_ORIENTABILITY_NOT_DIMENSION__H4_OF_M_iota_WITH_INTEGER_COEFFICIENTS_IS_ZERO_WHILE_H4_OF_S3_TIMES_S1_IS_Z_SO_M_iota_IS_CLOSED_AND_NON_ORIENTABLE_CARRIES_NO_FUNDAMENTAL_CLASS_AND_THE_INTEGRAL_OF_c2_THAT_C127_SECTION_5d_COMPUTED_IS_NOT_DEFINED_ON_IT_AT_ALL_A_FACT_PRINTED_IN_C129s_OWN_TABLE_AND_FLAGGED_BY_C129s_OWN_SKEPTIC_AS_THE_ORIENTABILITY_DIFFERENCE_AND_NEVER_CONNECTED_TO_RELEVANCE__THREE_READINGS_DO_MAKE_A_4_MANIFOLD_BUILT_FROM_THE_INTERNAL_S3_ANOMALY_RELEVANT_CHERN_SIMONS_LEVEL_QUANTIZATION_A_Q_MOD_Z_COEFFICIENT_ON_A_COBOUNDARY_AND_APS_SPECTRAL_FLOW_BUT_ALL_THREE_REQUIRE_AN_ORIENTABLE_SPIN_4_MANIFOLD_AND_ALL_THREE_PRODUCE_C127_SECTION_5ds_S3_TIMES_S1_NOT_M_iota__SO_THE_SPLIT_IS_BETWEEN_THE_TWO_OBJECTS_AND_C128_SECTION_6s_SUBSTITUTION_OF_M_iota_FOR_S3_TIMES_S1_IS_THE_STEP_AT_WHICH_THE_OBJECT_LEFT_EVERY_READING_THAT_COULD_HAVE_JUSTIFIED_IT_THOUGH_C128_MADE_IT_FOR_THE_GOOD_REASON_THAT_THE_RELATING_MAP_IS_THE_ORIENTATION_REVERSING_iota__C130s_FIRST_PREMISE_FAILS_BECAUSE_C127_SECTION_4s_LIST_IS_NON_EXHAUSTIVE_OMITTING_4D_TOPOLOGICAL_TERMS_WHICH_NEED_OMEGA_4_AND_OMEGA_5_THOUGH_THAT_READING_IS_SEPARATELY_CLOSED__C130s_SECOND_PREMISE_FAILS_BECAUSE_THE_NO_3D_THEORY_GREP_SEARCHED_A_STRING_AND_CANNOT_SEE_CS_3_WHICH_C123_CARRIES_AND_WHICH_C127s_OWN_ROW_X4_LEAVES_OPEN_BUT_CS_3_IS_A_NUMBER_NOT_A_QUANTUM_FIELD_THEORY_SO_DRAFT_2s_INVERSION_ON_THAT_TERM_SHIFT_IS_WITHDRAWN__NEW_AND_UNRECORDED_C127_IS_INTERNALLY_INCONSISTENT_PUTTING_A2_AT_OMEGA_5_IN_SECTION_5b_AND_AT_OMEGA_4_IN_SECTION_5d__C128_C129_C130_MATHEMATICS_UNTOUCHED_BUT_THEIR_BEARING_ON_OB1_IS_UNKNOWN_AND_THIS_ROUND_MAKES_IT_LESS_LIKELY_NOT_MORE__THREE_ROUNDS_WERE_NOT_WASTED_IS_WITHDRAWN`

**Status:** OB1 stays `PARKED`. No reopen condition met. C125's `FALSIFIED`,
C126's `WEAKENED`, C127's `BLOCKED`, C128's `OUTCOME_B`, C129's and C130's
verdicts stand as **mathematics**, none re-litigated. **C130's W1 resolves to
branch (ii) FOR `M_ι`** -- with both of C130's stated premises replaced.
**C127's X4 (`S³`-internal CS) is promoted**: it is the row that houses the
three surviving readings, and it is about `S³×S¹`, not `M_ι`. C127's X6/A2 and
C129's Z2 remain BLOCKED on round95 and `G`.

**Completeness:** `PARTIAL`, in eight named respects.

1. **No anomaly is evaluated.** No mechanism, no `t`-selector.
2. **Whether any of §6's three surviving readings says anything about `t` is
   `[UNKNOWN]`.** C121 (`η(D^t)`, NULL) and C123/C124 (`CS₃`, even in `t↔1−t`;
   absent from the reduced output; not a covariant tensor) already probed that
   territory. And C127 §5d's concession stands: as a number, `∫c₂ = −1`
   **restates** C126's winding.
3. **Whether C128-C130's work bears on OB1 is `[UNKNOWN]`** and this round
   supplies no positive case for it. Draft 2's claim that it does is withdrawn.
4. The KK-specific convention `claim.md` asked for is **`[UNKNOWN]`** (§2c) --
   `claim.md`'s permitted outcome, exercised.
5. **§6's (S7) rests on the APS spectral-flow identity, `[CITED]`, not
   re-derived.** It is that reading's weakest link and pass 3 named it.
6. `H^*(M_ι)`, `w₁ ≠ 0`, `w₂ = 0` are `[CITED]` from C129/C130. §5a re-derives
   the `𝔽₂` Betti vector by hand; **`w₁ ≠ 0` is NOT re-derived here.**
7. **Neither retrieved paper was read in full.**
8. **This final draft has NOT itself been skeptic-reviewed.** Passes 1-2 reviewed
   draft 1; pass 3 reviewed draft 2. §14 records this as the round's top
   residual risk and recommends a fourth pass **before** any registry edit.

**Gate fields assessed:** `PARENT_ACTION_GATE.md` **F4**, and only by correcting
a pre-filter's reason -- no mechanism, no gate-field value changes. F1 reused by
citation. F6 named (X4's own kill criterion asks about it), not assessed.

---

## 0. ⚠️ THREE FL Step 8a skeptic passes

All context-blind (`Agent(skeptic, model=opus)`), given `claim.md`,
`decision.md`, the script and the JSON, plus read access to the C123/C124/C127/
C128/C129/C130 files this round makes factual claims **about** (in scope,
because the claims are about them). No session history, no reasoning chain.

**Substrate note, per the Substrate Gate.** None of the three had Bash, web or
arXiv access -- Read/Write/Edit only. All three said so and none scored the gap
against the document. Every arithmetic check they report is **hand** derivation
against the committed JSON (pass 1 and pass 3 each hand-recomputed all four
PARTs and every gate). That makes their confirmations genuinely independent of
this round's code -- **and it is why the decisive tests had to be named by them
and run by this session.**

### 0a. What each pass did to the round

| pass | prompt register | reviewed | verdict | effect |
|---|---|---|---|---|
| **1** | formal | draft 1 | `WEAKENED` | Named the test that falsified draft 1 (the definition of `Z`). Found the `§3b`-undermines-its-own-evidence contradiction, the asymmetric existence standard, three citation defects, and six code defects. |
| **2** | plain | draft 1 | `WEAKENED` | Independently named the same test. Found the `CS₃` grep false negative, the pre-registered-`BLOCKED`-overridden defect, and supplied the two missing steelman readings (S6, S7). |
| **3** | anti-over-correction | draft 2 | **`FALSIFIED`** | **Found the orientability obstruction that is now this round's headline.** Showed draft 2's three "surviving" readings contain a manifold `M_ι` is not; that draft 2's layer mechanism contradicts C127's own placement and would excuse any dimension mismatch; and that draft 2 discarded a true claim along with the false ones. |

**Passes 1 and 2 were CONCORDANT** with each other (both `WEAKENED`, both
proposing the same test), so no tie-breaker was needed between them. **Pass 3
disagrees with the direction those two produced.** Per
`falsification-ladder.md`'s Paraphrase-Sensitivity Probe, the default on
disagreement is **the more skeptical verdict**, and pass 3 is it. It is also
correct on the merits: its two load-bearing facts were verified against source
this session (§0c) before being adopted.

### 0b. The two tests that turned the round

| | test | who named it | result |
|---|---|---|---|
| **draft 1 → draft 2** | *"read the definition of `Z` in arXiv:2512.17906 §4.1"* | passes 1 and 2, independently | **FIRED.** `Z` is *"some smooth auxiliary space `Z` with boundary `∂Z = ∂X`"* -- a **4-manifold bounding the internal 3-manifold** -- and the paper calls the integral over it *"the action of 3D Spin-Chern-Simons theory"*. So a 4-manifold built from an internal 3-manifold **is** anomaly-relevant, which draft 1 had denied. |
| **draft 2 → draft 3** | *"`M_ι` is closed and non-orientable; check whether it is an element of the bordism groups those readings use"* | pass 3 | **FIRED.** `H₄(M_ι;ℤ) = 0` (C129's own table). No fundamental class ⟹ no `∫c₂`. Every reading in §6 needs an orientable/Spin 4-manifold and produces `S³×S¹`. **Draft 2's readings vindicate C127 §5d's object and exclude C128-C130's.** |

**Both tests were named by a pass that could not run them, and run by this
session.** That is the Paraphrase-Sensitivity Probe doing exactly the work it is
for, twice, in opposite directions.

### 0c. Pass 3's load-bearing facts, verified against source before adoption

| claim | verification |
|---|---|
| `H₄(M_ι;ℤ) = 0`, `H₄(S³×S¹;ℤ) = ℤ` | `[VERIFIED, direct read]` C129 §4's table, rows 1 and 3; and C129's own text: *"`H₄(·;ℤ) = 0` vs `ℤ` (**the orientability difference**)"*. |
| C127 has **two** carve-outs, with mapping torus and spectral flow in the **second** | `[VERIFIED, direct read]` C127 §8: *"either a **differential/secondary** invariant (Chern-Simons, APS `η`, holonomy) **or** an invariant of the **relation between two backgrounds** (mapping torus, spectral flow, relative index). **Both carve-outs are part of the pearl.**"* Draft 2's §3e merged them, and mis-paraphrased C127 §2 as naming *"the only layer"*. |
| C127 X4 is open and C124 does not close it | `[VERIFIED, direct read]` X4: *"C124 killed the **13D-covariant** CS route. Does an `S³`-internal one survive F6?"*; C124 lists F6 as not assessed. **This one goes in draft 2's favour and is retained.** |
| `∫_{S³}CS₃(ω^t)` is a number, not a QFT | `[VERIFIED, direct read]` C123: it is `K(⅔x³ − ½x)`, entering as the coefficient `κ(t)` of a 4D term; and C123 records that whether it even has the large-gauge periodicity is *"a **SEPARATE, unanswered question** -- named here, not attempted."* |

### 0d. Findings from passes 1 and 2 that stand regardless of direction

*(These were accepted into draft 2 and are retained; they are defects of draft 1
that neither the inversion nor its reversal touches.)*

| # | Finding | Response |
|---|---|---|
| **A2** | Draft 1's §3b established a second `Ω₄` home and then kept using §4's silence as evidence: *"a 'no home in categories 1–N' argument needs a theorem that the N categories cover the space. The document itself proves they do not."* | **ACCEPTED.** §3b now states it as a **refutation of C130's stated reason**. The conclusion survives via §6 (S2), not via §4's list. |
| **A3** | Asymmetric existence standard: the 3D reading killed on a documentary criterion, the 13D replacement granted on a subject-matter one -- while C127 §3a quotes *"no consistent 13D parent theory is claimed"*. | **ACCEPTED**, §3f. The 14-manifold is presented as one reading, never "the correct object". |
| **A6** | *"the conclusion flips outright"* oversells: a probe admitting neither `Pin` structure is **not an element** of the bordism group, and nothing flips for **spin** probes. | **ACCEPTED**, §5d; the phrase is deleted from header, body, script docstring, and the `VERDICT_INPUTS` field is renamed. |
| **A7** | `G08` was a readback, not a reproduction. | **ACCEPTED, repaired** (`ring_point()`), **and pass 3 showed the repair is cosmetic w.r.t. the complaint** -- so `G08` is now labelled a consistency re-run, not discriminating (§9b). |
| **A8** | §9b's "every gate, exactly once" omitted `G03`. | **ACCEPTED**, §9b now covers all 21. |
| **B2** | **Mechanism-transfer gate violation:** *"literally a quotient of `S³`"* is a **name** match, not a **role** match (link at infinity of a non-compact cone vs. compact internal factor). | **ACCEPTED**, sentence deleted, transfer marked `[WEAK]` in §2b. |
| **B3/B4/B5** | Three citation defects: a colon silently converted to a period inside a "verbatim" quotation of C127 §3 (dropping *": an anomaly constrains field content, never a background connection parameter"*); a dropped exculpatory clause from C128 §6 (*"or conditional on a compactification this project does not have"*); and a splice of C130 Pearl 2's text into a quotation attributed to W1. | **ACCEPTED, all verified verbatim, all repaired** (§4b, §8). Draft 1's root-cause narrative against C128 §6 is **withdrawn as unfair**. |
| **B6/C9/C8/C10** | Four code defects: two unlinked encodings of `H²(M_ι) = 0`; the Künneth cross term untested; the `F₂` monodromy block never exercised; a dead conditional in the injection. | **ACCEPTED, all repaired** -- `G15`, `G16`, `G17`, `G18`, plus `G19` from pass 3. |
| **C1** | Draft 1's verdict line claimed two skeptic passes while its own §0 said they had not run -- verbatim recurrence of C128's finding C1, in the file that cites C128's C1 as the reason for its shape. | **ACCEPTED, recorded permanently.** Writing "NOT YET RUN" documented the defect; it did not cure it. |
| **C2** | The pre-registered `BLOCKED` condition was met and overridden. | **ACCEPTED.** §2c now returns `[UNKNOWN]` on the KK-specific question. |
| **C4** | The `CS₃` grep false negative. | **ACCEPTED** for what it shows (§3c) -- **but draft 2's inference from it, that the project therefore has a 3D theory, is withdrawn per pass 3 (§3c).** |
| **C7** | Draft 1 elided *"on `M_ι`"* from C130's headline quotation, making C130 look like it over-claimed when it explicitly did not. | **ACCEPTED**, restored (§5d). C130 is credited with raising the manifold question itself, at impact 8. |
| **C11** | `M_ι` is an overloaded symbol (C128 §2b's frame transition function vs. the 4-manifold). | **ACCEPTED**, §7(d) -- **and the script contained the same defect** (`"point"` denoting a 10-manifold in PART 2 and a 4-manifold in PART 3), found by pass 3 and repaired. |
| **C12** | *"neither branch was preferred"* was not credible given the round was generated by C130's impact-8 pearl. | **ACCEPTED**, §1. |

### 0e. Pass 3's findings

| # | Finding | Response |
|---|---|---|
| **D1** | **THE FINDING. The three "surviving" readings are each about a 4-manifold `M_ι` demonstrably is not.** *"`M_ι` is **closed** and **non-orientable** … (S6) — `∂Z = ∂X`. `M_ι` has no boundary. (S1)/(S3) — `Ω₄` of closed **Spin** 4-manifolds; `M_ι` is not orientable. (S7) — the APS identity uses `Y × S¹` clutched by the gauge transformation — orientable, spin. **That is C127 §5d's `S³ × S¹`, and this file's own §7(a) says it is a different manifold from `M_ι`.**"* | **ACCEPTED ENTIRE, verified (§0c), and it is now the round's headline.** Draft 2's *"three rounds were not wasted"* and *"ON-TARGET"* are **withdrawn**. The sharpened form: `H₄(M_ι;ℤ) = 0`, so `∫c₂` -- the number C127 §5d computed -- **is not defined on `M_ι`**. §3a, §6. |
| **D2** | **§3e's layer mechanism is a relabelling and contradicts C127's own placement.** C127 §8 has **two** carve-outs and puts *mapping torus, spectral flow* in the **second**; draft 2 merged them into the first. *"since every Dai-Freed argument has a differential evaluation on top of a topological class, any object at any wrong degree can be re-described as 'differential layer.' A distinction with no discriminating power is not a mechanism."* | **ACCEPTED, and §3e is WITHDRAWN entire**, together with draft 2's Pearl 1 built on it. Pass 3's replacement diagnosis is adopted: *"What [§4's list] omits is **a theory** …, not a **layer**."* |
| **D3** | **A paraphrase presented as a quotation, in the verdict string.** Draft 2's header quoted C127 §2 as naming *"the only [layer] in which anything has ever shown `t`-sensitivity"*; C127 §2 states a **disjunction**, and the dropped disjunct is the one housing the mapping torus. Verbatim recurrence of B3/B4 **in the rewrite that logged B3/B4**. | **ACCEPTED**, deleted. Recorded as a third instance of the same species in one round. |
| **D4** | **The `Z` retrieval refutes a broader claim than the one draft 1 wrote.** `Z` is a **coboundary**, not a mapping torus and not a bordism class of the internal manifold; and the anomaly is an invariant **of `∂Z`**, not of `Z`. So draft 1's literal §2b/§2c sentences were **not** refuted, and draft 2 over-withdrew. | **ACCEPTED.** §2b now separates the literal sentences (which survive) from the over-broad **implication** drawn from them (which does not). |
| **D5** | **Bulk-independence cuts against the rescue.** *"the reading the round leans hardest on to rescue `M_ι`-specific work simultaneously certifies that no such specifics can matter."* | **ACCEPTED**, §6 (S6). This is independent of D1 and reaches the same place. |
| **D6** | **"3-dimensional object" ≠ "3D theory".** C123's `∫_{S³}CS₃(ω^t)` is a number entering as a 4D coefficient; §2b's primary can call its object a 3D TQFT because it has a level and a partition function. **The shift is undefended and (S1)/(S3)/(S6)/(S7) all require it.** | **ACCEPTED**, §3c rewritten. C130's second premise is **restored in substance** (no 3D QFT) while remaining wrong as verified (the grep could not have shown it). |
| **D7** | **§3b's second `Ω₄` home is closed by draft 2's own (S2)**, so it weakens C130's reason without restoring relevance. | **ACCEPTED**, §3b now says exactly this. |
| **D8** | **(S7) silently assumes the C126 branch** of C127 §5a's unreconciled ingredient 1 (*"under the C125 reading there is no gauge transformation to be anomalous about at all"*), which C128 §5 resolved **against**; and its content is `∫c₂ = −1`, already conceded a restatement. | **ACCEPTED**, §6 (S7) now states the assumption and the concession. |
| **D9** | **Code:** `G15`-`G18` are real repairs with demonstrable failure modes; **repair #4 (`G08`) is cosmetic** w.r.t. the complaint it answers, and §9b promoted it to "genuinely discriminating" -- a reclassification the repair does not earn. `TOTAL_SW["M_iota"]` is an unlinked **third** encoding. The key `"point"` denotes two different objects. And *"34/34 on the first run is a skeptic trigger, not a result"* (C130 §12) was not applied to this round's own 20/20. | **ACCEPTED all.** `G08` demoted to a consistency re-run in §9b; `G19` links the third encoding; `"point"` renamed in PART 2; the 21/21 is explicitly **not** offered as evidence (§9b). |
| **D10** | **Hold the registry edit.** | **ACCEPTED**, §10.11 and §14. |

**Nothing dismissed. Four findings answered rather than accepted outright:**
pass 1's B7 (PART 1 retained, correctly labelled); pass 2's proposal of a bare
`BLOCKED` (partially adopted -- §2c only); pass 3's implication that §3b should
be dropped (retained, because C130's *reason* being wrong is a real finding even
when its conclusion survives); and pass 3's *"W1 should return to `OPEN`"*
(**answered**: W1's own dichotomy is about whether a reading exists in which a
4-manifold is the right home. One does -- for `S³×S¹`. That resolves W1 rather
than reopening it, and the resolution is branch (ii) **for the manifold W1 is
about**, which is `M_ι`).

---

## 1. Zero-Signal Gate (Step −5)

| Field | Status | Content |
|---|---|---|
| **Entity** | **PASS** | *"C127 §4's dimension audit and C127 §5d's mapping-torus construction are answering the same dimensional question"* -- a checkable proposition about two named sections of one file. |
| **Falsifiable predicate** | **PASS** | Binary and pre-registered; both branches writable in advance. |
| **Measurable outcome** | **PARTIAL** | `claim.md` asked for the convention *"applied to an internal `S³` factor of a **Kaluza-Klein compactification**"*. That is **`[UNKNOWN]`** (§2c) -- `claim.md`'s permitted outcome, **exercised**. What was obtained: the convention for the anomaly of a QFT `[VERIFIED-tool]`; the existence of the coboundary construction `[VERIFIED-tool]`; and the orientability obstruction `[VERIFIED]`, which turned out to decide the case without needing the KK-specific convention at all. |

**Pre-registration, honestly assessed** (skeptic C12): the round exists because
C130's Pearl 2 scored one direction at impact 8, and draft 1 returned it. Draft
1's *"neither branch was preferred"* is **withdrawn** as not credible. What can
be said is a fact rather than a self-assessment: **the round changed direction
twice, both times because a context-blind pass named a test the author had not
run.** Neither change came from the author.

---

## 2. Steps −4 / −3 -- literature

### 2a. The convention for the anomaly OF A QFT

**[VERIFIED-tool, PRIMARY -- read this session]** arXiv:1808.00009v3 §2.1.1-2:

> *"Suppose we are interested in studying anomalies on a fermion theory defined
> on some manifold `X` …"* · *"One constructs an auxiliary `(d+1)` dimensional
> space as the quotient `X×[0,1]/r` …"* · *"… a non-contractible closed path on
> the space of connections on **the theory on `X`** …"* · *"The `d`-dimensional
> theory will only be anomaly free if a certain topological invariant
> constructed out of a particular `(d+1)`-dimensional Dirac operator … actually
> vanishes."*

`X` is the theory's own manifold; the mapping torus sits at `d+1`. Witten's
`SU(2)` anomaly: `X = S⁴`, torus of dimension 5, `Ω^{Spin}_5(BSU(2)) = ℤ/2`.

**This survived all three passes and all three drafts.** Pass 3: *"§3a is
correct, and both drafts agree on it … This is the RIGHT half and I could not
break it."* Its consequence: §5d's invocation of the construction, for an
unnamed theory, was a **mislabelling** -- neither of this project's two QFTs
gives degree 4.

*(Draft 1 called the shared citation between §4 and §5d *"the single sharpest
reason"*; pass 1's A9 is right that "same citation ⟹ same question" does not
follow. Demoted to what it is: evidence about §5d's **label**.)*

### 2b. The coboundary construction -- what it does and does not show

**[VERIFIED-tool, PRIMARY -- §3 and §4.1 read this session, AFTER passes 1 and 2
named the test]** arXiv:2512.17906v2.

**Role first, per skeptic B2:** this is **geometric engineering / SymTFT**, not
KK reduction. `X = C²/Γ_ADE` is a **non-compact cone**, `∂X = S³/Γ_ADE` its
**link at infinity**, the 7D theory transverse to it. **Transfer is by mechanism,
not by role: `[WEAK]`.**

> *"reducing (2.3) on **some smooth auxiliary space `Z` with boundary
> `∂Z = ∂X`** … `α = ½∫_Z T₂(T₂ + w₂) mod 1`."*
> *"any integral lift of the second Stiefel-Whitney class is a characteristic
> vector on `H²(Z';ℤ)` where `Z'` is **closed 4-manifold**. In this context,
> (4.11) should be understood as **the action of 3D Spin-Chern-Simons theory**
> … and the independence on the choice of bulk follows standard arguments by
> considering the difference of two bulk choices."*

**What this establishes:** a **4-manifold built from an internal 3-manifold** can
carry anomaly-relevant data. Draft 1's *implication* -- that no 4-manifold home
exists for internal data -- is therefore **false**, and that is why draft 1 was
right to be overturned on that point.

**What it does NOT establish, per pass 3's D4 and D5, and this is where draft 2
went wrong:**

* `Z` is a **coboundary**, not a mapping torus and not a bordism class of the
  internal manifold. Draft 1's literal §2c sentence -- *"the **mapping torus /
  bordism class** of a compactification's internal manifold is itself the home
  of an anomaly"* -- is **not** refuted by it, and draft 2's blanket withdrawal
  of that sentence is itself withdrawn.
* The invariant belongs to **`∂Z`**; `Z` is scaffolding, and *"the independence
  on the choice of bulk"* is the paper's own guarantee that **`Z`-specific data
  cannot matter.** So this reading cannot rescue `M_ι`-specific computations --
  it certifies they are immaterial.
* `M_ι` is **closed**. It cannot be `Z`. Nor can it be `Z'`: a closed `Z'` here
  is `Z₁ ∪ (−Z₂)` glued along a Spin 3-manifold, hence **orientable**, and
  `M_ι` is not.

### 2c. The KK-specific question -- `[UNKNOWN]`

Searched (arXiv full-text, web) for a construction in which the mapping torus or
bordism class of a **compact KK internal manifold** is an anomaly's home at
degree `dim+1`. **None retrieved.** Draft 1 read this as evidence the convention
is uniform; after §2b that inference is unavailable, and the search's scope was
set by a role-transfer the mechanism-transfer gate forbids. **`[UNKNOWN]`, and
`claim.md`'s permitted outcome is exercised here.** The round does not need it:
§3a settles the label and §6/D1 settles the object.

**Not consulted:** Freed-Hopkins; Witten 1982; Dai-Freed's original;
Kirby-Taylor (C129 did); ABP67/Gia71. Neither paper read in full.

---

## 3. The answer

### 3a. The real obstruction: `M_ι` carries no fundamental class

`[VERIFIED, direct read of C129 §4's table]`

| | `H_*(·;ℤ)` | orientable? | `∫c₂` defined? |
|---|---|---|---|
| **`S³×S¹`** (C127 §5d's object, `deg = +1`) | `ℤ, ℤ, 0, ℤ, ℤ` | **yes**, spin | **yes** -- and `= −1` |
| **`M_ι`** (C128 §6b's substitute, `deg = −1`) | `ℤ, ℤ, 0, ℤ/2, **0**` | **no** (`w₁ ≠ 0`) | **NO** |

`H₄(M_ι;ℤ) = 0`: there is **no integral fundamental class**, so
`∫_{M_ι} c₂` is not defined. **The very number C127 §5d computed cannot be
evaluated on the manifold C128-C130 analysed.**

C129 printed both rows and its own skeptic pass flagged the difference --
*"`H₄(·;ℤ) = 0` vs `ℤ` (**the orientability difference**)"* -- as a
*consistency* observation. **Nobody connected it to the relevance question**,
including C130, both prior drafts of this file, and passes 1 and 2.

> **This is the round's principal finding, and it is due to skeptic pass 3.**

### 3b. C130's first premise fails: C127 §4's list is non-exhaustive

By the Anderson-dual sequence C127 §4 itself quotes, **`Ω₄` is the lower term
for 4-dimensional invertible theories -- 4D topological terms -- which need
`Ω₄` and `Ω₅`.** §4 lists both degrees for 13D topological terms and both for
the 4D anomaly, and **omits the 4D topological-terms row**, the one containing
`Ω₄`. So `Ω₄`'s absence from the list is an **omission**, not the absence of a
home. `[INFERRED]` from `[VERIFIED-tool]`-cited Freed-Hopkins.

**But this does not restore relevance** (pass 3, D7): the home it supplies is
separately closed at §6 (S2). **C130's conclusion survives; C130's argument does
not.** Recorded because a surviving conclusion reached by a bad argument is
exactly what this project's own gates exist to catch.

### 3c. C130's second premise: wrong as verified, right in substance

`[VERIFIED-tool, grep]` Draft 1 verified *"no 3D theory"* by grepping the
**strings** `3d theory | 3d qft | …`. The project calls the object **`CS₃`**:
`grep -rl -E "CS₃|CS_3"` returns C123, C124, C127, `OPEN_BLOCKERS.md`,
`PARENT_ACTION_GATE.md`; C123 carries
`S_mix = k∫CS₃(ω_{S³})∧P₄(M₄)∧ch₃(E_{S⁶})` and `∫_{S³}CS₃(ω^t)`. And
`[VERIFIED, direct read]` **C127's row X4 leaves the `S³`-internal CS route
explicitly OPEN**: *"C124 killed the **13D-covariant** CS route. Does an
`S³`-internal one survive F6?"* -- with C124 listing F6 as not assessed.

> **So the grep could not have established what it was cited for.** That defect
> stands, and Pearl 2 is about it.

**But draft 2's inference from it is WITHDRAWN** (pass 3, D6). For `Ω₄` to be a
3D theory's anomaly home one needs a 3D **quantum field theory** -- a partition
function, a level. `[VERIFIED, direct read]` C123's `∫_{S³}CS₃(ω^t)` is
`K(⅔x³ − ½x)`, a **number**, entering as the coefficient `κ(t)` of a 4D
vacuum-energy term; and C123 records that whether it even carries the
large-gauge periodicity is *"a **SEPARATE, unanswered question** -- named here,
not attempted."* §2b's primary can call its object a 3D Spin-Chern-Simons
**theory** because it has a level and a partition function; a classical
functional evaluated at one `t` on one fixed background is not that.

> **Net: C130's premise is true (no 3D QFT) and its verification was invalid.
> Draft 2 inverted the round on the gap between those two facts.**

### 3d. A previously unrecorded inconsistency inside C127

`[VERIFIED, direct read]` §5b fixes A2 on `Ω^{Spin}_5(B G_eff)`; §5d says of its
`Ω₄` class that *"evaluating the anomaly homomorphism … on this class … is
exactly A2"*. **The same question sits at degree 5 and degree 4 in one file.**
Unrecorded by C127's own skeptic pass, by C128/C129/C130, and by passes 1 and 2;
independently confirmed by pass 3 (*"real and, as far as I can check, new"*).

### 3e. *(withdrawn)*

Draft 2 argued that §4 and §5d address two **layers** -- topological/invertible
vs differential/secondary. **Withdrawn entire** (pass 3, D2). C127 §8 keeps
**two** carve-outs and places *mapping torus, spectral flow, relative index* in
the **second**, which draft 2 merged into the first; and the second carve-out
does not answer C130, because a bordism class of a *relation* still sits in some
`Ω_d` whose `d` must match a theory's dimension. Worse, the mechanism had **no
discriminating power**: every Dai-Freed argument is a differential evaluation on
a topological class, so any object at any wrong degree could be re-described
into safety. **A distinction that excuses everything is not a mechanism.**

Pass 3's replacement is adopted: what C127 §4's list omits is **a theory** (the
4D-topological-terms row, §3b), **not a layer**.

### 3f. The existence standard, applied symmetrically

`[VERIFIED, direct read]` C127 §3a quotes the project's own file: *"no consistent
13D parent theory is claimed"*, *"There is no established `Spin(1,12)` structure
group in this project"*. So neither a 3D nor a 13D theory is "had" on a
documentary criterion. Draft 1 applied the documentary criterion to kill the 3D
reading and a subject-matter criterion to grant the 13D one; **corrected** --
§4's 14-manifold is one reading among several, never "the correct object".

---

## 4. The 14-dimensional object -- one reading, not the answer

### 4a. Factorisation lemma

**Lemma.** *For `X = A × B` and `f = id_A × h`, `M_f ≅ A × M_h`.*
**Proof.** `[(a,b,t)] ↦ (a,[(b,t)])`; the gluing does not move `A`. ∎

So for `f = id × ι × id` on `M₁₃`: `M_f = M₄ × S⁶ × M_ι`, dimension
`4+6+4 = 14 = 13+1`, and `Ω₁₄` is the second degree of C127 §4's *"a 13D
parent's own anomaly needs `Ω₁₄` and `Ω₁₅`"*.

`[VERIFIED]` -- explicit diffeomorphism, homology prediction checked on
`A = S¹, B = S¹, h =` the flip (predicts `S¹ × Klein bottle`; both sides give
`ℤ, ℤ²+ℤ/2, ℤ+ℤ/2, 0`; the `T³` control gives `ℤ, ℤ³, ℤ³, ℤ`).

**Its standing after pass 3:** unchanged as mathematics, and **it does not
rescue anything**. `Ω^ξ_{14}` for the twisted structure this project needs is
`[UNKNOWN]`; `Ω^{Spin}_{14} = 0` is the untwisted value; and the reading
inherits C127 §3a's "no consistent 13D parent theory" problem (§3f).

### 4b. C128 §6, stated fairly

C128 §6's preamble **in full**: *"as frozen, `M₄` is non-compact (C127 §3), so no
13D mapping torus is a closed manifold at all … Everything below is at the `S³`
level, **or conditional on a compactification this project does not have**."*

C127 §3 **in full**: *"… a statement about the admissible closed manifolds an
anomaly argument quantifies over … This is not a technicality -- it is the
reformulation**: an anomaly constrains field content, never a background
connection parameter.**"*

> **Draft 1's root-cause claim -- that C128 "took the wrong lesson" and made "a
> change of dimension by ten" -- is WITHDRAWN as unfair** (pass 1, B3/B4). C128
> named the 13D alternative in the same sentence, marked it conditional, and its
> §6d does 13D bookkeeping. And C127 §3's own gloss is about **field content vs
> background parameter**, not about probe manifolds.

What survives: a Dai-Freed argument quantifies over closed manifolds with the
required structure, so non-compactness of the physical `M₄` does not by itself
prevent forming the 14-dimensional object with a closed probe. Neither C127 nor
C128 said that. `[INFERRED]`.

---

## 5. What transfers to the 14-manifold *(relevant only to §4's reading)*

### 5a. The Betti input, re-derived by hand (Wang)

`M_ι` fibres over `S¹` with fibre `S³`; `f^* ≡ 1 (mod 2)`, so `f^*−1 = 0` in
every degree. The Wang sequence gives `H² = 0` (squeezed between `H¹(S³) = 0`
and `H²(S³) = 0`) and `(1,1,0,1,1)` overall. `[INFERRED]`. **This does NOT
re-derive `w₁ ≠ 0`** (pass 1, B8): `H¹ = 𝔽₂` is necessary, not sufficient.

### 5b. Transfers: non-orientability

`w₁(P) = w₁(M₄) + w₁(M_ι) ≠ 0` for orientable `M₄`. `[VERIFIED]`, `G12`.

### 5c. Does not transfer: the vanishing of the obstruction group

`H²(P;𝔽₂) = H²(M₄;𝔽₂) ⊕ (H¹(M₄;𝔽₂) ⊗ H¹(M_ι;𝔽₂))`:

| probe `M₄` | `S⁴` | `CP²` | `T⁴` | `K3` | `RP⁴` |
|---|---|---|---|---|---|
| `dim_{𝔽₂} H²(P;𝔽₂)` | **0** | 1 | 10 | 22 | 2 |

`[VERIFIED]`, `G16` (exact values, so the Künneth cross term is actually tested)
and `G17`.

### 5d. Probe-dependence -- not a flipped conclusion

`w₂(P) = w₂(M₄) + w₁(M₄)·w₁(M_ι)`; `w₁(P)² = w₁(M₄)²`.

| probe `M₄` | `Pin⁺` | `Pin⁻` |
|---|---|---|
| `S⁴`, `T⁴` (**spin**) | yes | yes |
| `CP²`, `RP⁴` | no | no |
| `M_ι` alone (C129/C130's case) | yes | yes |

> **Per pass 1's A6: a probe admitting neither structure is NOT an element of the
> bordism group** -- excluded from the domain, not a constraint found within it.
> For **spin** probes nothing changes. Draft 1's *"the conclusion flips
> outright"* is **withdrawn**. What survives: existence on `P` is a **real,
> probe-dependent condition**, so C129/C130's *reason* ("the group is zero,
> uniformly in `G`") does not transfer even where the verdict does.

**C130 never claimed otherwise:** its headline reads *"every `Ḡ = G/⟨z⟩`-bundle
**on `M_ι`**"*. Draft 1's ellipsis removed "on `M_ι`" (pass 2, C7); restored.

### 5e. Re-scoping table -- corrected after pass 3

| result | mathematics | bearing on OB1 |
|---|---|---|
| C128 §2a Maurer-Cartan; §4 `Diff`-theorem | **untouched** | unchanged (statements about maps of `S³`) |
| **C128 §6b** `M_ι` non-orientable, `Ω^{Spin}` wrong functor | **untouched, and CORRECT** | **and being correct is what costs the object its readings** (§3a, §6). C128 §6b is simultaneously the soundest step and the fatal one. |
| C129 §4 `H²(M_ι;𝔽₂) = 0`; §6 constructive `Pin` lift | **untouched** | `[UNKNOWN]`. Establishing that `M_ι` admits `Pin` structures is the **entry ticket to `Ω^{Pin}_4`** -- which is C130's own objection restated, not answered (pass 3, D1). |
| C130 §4 theorem; §5 `H²(M_ι;A) = 0` ∀`A`; §7 non-vacuity | **untouched** | `[UNKNOWN]`, same reason |
| C130's W1 *conclusion* | **stands, for `M_ι`** | — |
| C130's W1 *premises* | **both false** (§3b, §3c) | — |

> **Draft 2's *"their work is ON-TARGET"* and *"Three rounds were not wasted"*
> are WITHDRAWN.** The honest statement: their mathematics is correct and
> untouched; whether it bears on OB1 is `[UNKNOWN]`; and this round makes it
> **less** likely, not more, because the readings that would have housed it
> exclude their manifold.

---

## 6. Readings in which a 4-manifold is right -- and none of them contains `M_ι`

Seven readings were constructed across the three drafts (five by draft 1, two
supplied by pass 2). Their final status, after pass 3's D1:

| | reading | is a 4-manifold right? | is **`M_ι`** the manifold? |
|---|---|---|---|
| **S1/S3** | a 3-dimensional object on `S³` -- `CS₃` level quantization | **yes**, and C127's **X4** leaves it open | **NO** -- `Ω₄` here is of closed **Spin** 4-manifolds; `M_ι` is non-orientable. The object is `S³×S¹`. And no 3D **QFT** is on record (§3c). |
| **S6** | a `Q/Z` coefficient on a coboundary `Z`, `∂Z = S³` | **yes**, realized in §2b's primary | **NO** -- `M_ι` is closed, so it cannot be `Z`; and bulk-independence certifies `Z`-specific data is immaterial anyway (pass 3, D5). |
| **S7** | APS **spectral flow** of the `D_{S³,t}` family around the `∇⁰→∇¹` loop | **yes** -- and this is the reading that bears most directly on OB1 | **NO** -- the APS identity uses `Y × S¹` **clutched by the gauge transformation**: orientable, spin, i.e. `S³×S¹`. Also assumes C127 §5a's **C126** branch, which C128 §5 resolved **against** (pass 3, D8), and its content is `∫c₂ = −1`, already conceded a restatement. `[CITED]`, identity **not re-derived**. |
| **S2** | 4D topological terms of the effective theory (`Ω₄`, `Ω₅`) | degree yes | **NO** -- no `G_eff`-bundle datum exists for `M_ι` (C127 §0 finding 8(ii)'s type error, propagated by C130 §3b); granting one, the class is `σ`-symmetric so C127 §5b's A1 kills selection. *(Draft 1 closed this on "wrong manifold" grounds, which pass 1's A5 correctly showed contradicts §4b. Closure reason corrected; closure stands.)* |
| **S4** | Hamiltonian / spatial slice | **no** | `M₄` is the spacetime; `S³` is not a slice of it. |
| **S5** | the internal data as a twist at `Ω₅` | **no** (degree 5) | a parallel legitimate route -- C127 §5b / X1. |

> **The pattern is the finding.** Every reading that makes a 4-manifold right
> produces C127 §5d's **`S³×S¹`** -- orientable, spin, carrying `∫c₂ = −1`.
> **`M_ι` is in none of their bordism groups.** So the split runs between the two
> **objects**, and C128 §6's substitution is the step at which the object left
> every reading that could have housed it.

---

## 7. Precision corrections to the record

**(a) `S³×S¹` and `M_ι` are different manifolds, and the difference is now
load-bearing rather than cosmetic.** §5d's object is orientable and spin with an
`SU(2)` bundle clutched by the **gauge** transformation `g`; `M_ι` is the mapping
torus of the **diffeomorphism** `ι`, non-orientable, `H₄(·;ℤ) = 0`. C128 §6a
made the replacement deliberately and correctly (a mapping torus glued by
`g ∈ 𝒢` joins gauge-equivalent configurations by construction). **This round's
own `claim.md` conflates them**, and draft 1 treated the distinction as
immaterial *"since both are 4-dimensional"*. It is not.

**(b) C130's `Ω₄` characterisation** -- §3b. A refutation of the stated reason,
not a refinement; and it does not restore relevance.

**(c) C127's internal degree inconsistency** (§5b `Ω₅` vs §5d `Ω₄`) -- §3d. New.

**(d) `M_ι` is an overloaded symbol** -- C128 §2b's `O(3)`-valued frame
transition function vs. C129/C130's 4-manifold; C128 §6b never writes `M_ι`.
`research-methodology.md`'s open **gap #2**. **And this round's own script
committed the same defect** (`"point"` denoting a 10-manifold in PART 2 and a
4-manifold in PART 3), caught by pass 3 and repaired.

**(e) What C130 got right:** that the manifold needed checking; that the caveat
belonged in the verdict string; and, as it turns out, the conclusion. **Its
instinct was right, both its arguments were wrong, and the correct argument was
in C129's own table.**

---

## 8. Kill Analysis (Anti-Overfitting Gate)

### What this round KILLS

* **`M_ι` as an object in any reading that makes a 4-manifold anomaly-relevant
  here.** Not by dimension -- by **orientability** (§3a, §6). This is stronger
  and more specific than C130's flag, and it is the round's own content.
* **Both of C130's stated premises** (§3b, §3c), and their instances now standing
  in `PARENT_ACTION_GATE.md:341-343` and `pearl_registry/INDEX.md:130`.
* **Draft 1**: its §2b implication, §3c grep closure, §4b root cause, (S2)/(S3)
  closure reasons, "flips outright", and its verdict.
* **Draft 2**: its layer mechanism (§3e), its *"three rounds were not wasted"*,
  its blanket withdrawal of draft 1's literal §2c sentence, and its inversion of
  C130's second premise.
* **The reading in which `M_ι` is the `(d+1)` Dai-Freed mapping torus of a
  `d`-dimensional QFT this project names** (§2a, §3a) -- the one thing all three
  drafts agree on.

### What this round does NOT kill

* **Any mathematics in C128, C129 or C130** -- untouched; C128 §6b in particular
  is correct, and that is the point.
* **C127's `BLOCKED`**, C125's, C126's, C128's, C129's, C130's verdicts.
* **C127's X4** -- the `S³`-internal CS route, now the row that houses §6's
  surviving readings, open and unattempted.
* **C127's A2 / X6**; C129's Z2; C130's W2-W5.
* **Whether a `t`-selector exists**; round95; `G`; Family C / `ε₄ε₆`;
  round80 / Round117.
* `N_gen=3`'s CONDITIONAL status, `lambda = FREE_COUPLING_PARAMETER`,
  `sm_derivation_claimed = False`, `safe_for_runtime = False`, OB1's `PARKED`.

### Relaxation Map (one assumption changed per variant; none attempted here)

| Variant | Single assumption changed | Kill criterion |
|---|---|---|
| **V1** *(promoted -- it is C127's own X4)* | Work with **`S³×S¹`**, the orientable object, instead of `M_ι`: `∫c₂ = −1` is defined there, and §6's three readings all live there | Does any of them say anything C121 (`η`, NULL) and C123/C124 (`CS₃`, even in `t↔1−t`) did not? **The thing to beat is C127 §5d's own concession: as a number the class restates C126's `n = −1`.** Prerequisite: C127 §5a's ingredient 1, which C128 §5 resolved **against** the C126 branch this needs -- so V1 must first say why it may use it. |
| **V2** | Build the **spectral-flow** reading (S7) explicitly on `S³×S¹` | Is it `t`-odd, or blind to which member of the pair (as C127 §5b's A1 predicts for every `σ`-symmetric object)? **Re-derive the APS identity rather than citing it.** |
| **V3** | Accept `M_ι` as the right object and ask what invariant a **non-orientable** closed 4-manifold with `H₄(·;ℤ) = 0` can carry | `Pin` bordism `Ω^{Pin^±}_4` is exactly that setting, and C129/C130 supplied the structures. **This is the one route on which their work is directly reusable** -- but it needs a physical question whose answer lives in `Ω^{Pin}_4`, and §6 says none of this project's does. Find one or close the route. |
| **V4** | The **13D** reading of §4 with a closed probe, `Ω^ξ_{14}` | Probe-dependent (§5d); `Ω^{Spin}_{14} = 0` is untwisted and does not settle it; inherits C127 §3a's "no consistent 13D parent theory". |
| **V5** | Audit C127 X1-X6, C128 Y1-Y5, C129 Z1-Z5, C130 W1-W5 for **object** confusion (`S³×S¹` vs `M_ι`) as well as degree | W1 is now a confirmed instance in **three** directions (C130's reason; draft 1's over-reach; draft 2's inversion). C130's Pearl 3 predicted hand-off tables propagate unchecked inferences. |

---

## 9. Verification

**Gate: 21 of 21 pass.** `ruff check` clean, `ruff format` applied. Stdlib only;
no project code imported. **Per C130 §12's own standard -- *"34/34 passing on the
first run is a skeptic trigger, not a result"* (pass 3, D9) -- the 21/21 is NOT
offered as evidence for anything.** What it does is fix the round's arithmetic;
the round's conclusion rests on §3a and §6, neither of which the script touches.

### 9a. Defects in this round's own artifacts

| # | defect | how caught | fix |
|---|---|---|---|
| 1 | `IndexError` at `n = top+1` | crash | guarded accessor |
| 2 | precedence accident in `betti_f2` | read-back | rewritten |
| 3 | **PART 1's control could not fail** (`T³` and `S¹×K` share `F₂` Betti) | **`G02` fired** | integral routine + Smith normal form; the impossibility is now itself gated (`G02b`) |
| 4 | `G08` a readback | passes 1/2 | `ring_point()`; **pass 3 showed the repair cosmetic** → `G08` demoted to a consistency re-run in §9b |
| 5-8 | unlinked encodings; untested Künneth cross term; unexercised `F₂` monodromy block; dead conditional in the injection | passes 1/2 | `G15`, `G16`, `G17`, `G18`, rebuilt injection |
| 9 | **a third unlinked encoding** (`TOTAL_SW["M_iota"]`); **`"point"` overloaded** across PART 2 and PART 3 | pass 3 | `G19`; PART 2's key renamed `S6_and_M_iota_only` |
| 10 | **verdict line claimed passes that had not run** | pass 2 | recorded permanently (§0d, C1) |
| 11 | **draft 1's conclusion was wrong** | passes 1+2 named the test; this session ran it | rewritten |
| 12 | **draft 2's conclusion was wrong** | pass 3 named the test; this session verified it | rewritten |
| 13 | **draft 2 presented a paraphrase of C127 §2 as a quotation, in the verdict string** -- third instance of the truncation species in one round, in the draft that logged the first two | pass 3, D3 | deleted |

### 9b. Honest accounting of every gate, exactly once (21)

* **Arithmetic identities, not evidence:** `G03`, `G04`, `G05`.
* **Reproduction of a textbook fact (checks the tool):** `G02c`.
* **Provably-cannot-fail, gated AS SUCH:** `G02b`.
* **Consistency links / re-runs, NOT discriminating:** `G15`, `G19` (the three
  encodings of `w₂(M_ι) = 0`); **`G08`** -- *demoted here per pass 3's D9: with
  two point factors `tensor3` is the identity on `M_ι`'s data, so `w₂ = ∅` still
  follows from `TOTAL_SW["M_iota"]` having no degree-2 entry. Only its
  `non_orientable` conjunct can fail.*
* **Genuinely discriminating:** `G01` (**`Z` half only** -- the `F₂` half cannot
  fail, pass 1 B7), `G02`, `G06`+`G07`+`G16`+`G17`, `G09`+`G10`, `G11`, `G12`,
  `G18`.
* **Injections, headline-moving:** `G13`, `G14`.

### 9c. Evidence tiers

| Tier | Claim |
|---|---|
| `[VERIFIED]` -- direct read this session, and it is the round's headline | §3a: `H₄(M_ι;ℤ) = 0` vs `H₄(S³×S¹;ℤ) = ℤ` (C129 §4's own table and its own "orientability difference" note). |
| `[VERIFIED-tool, PRIMARY]` | §2a's convention (arXiv:1808.00009v3 §2.1.1). §2b's coboundary construction (arXiv:2512.17906v2 §3, §4.1) -- **role differs from this project's; transfer `[WEAK]`**. |
| `[VERIFIED-tool, grep]` | §3c's `CS₃` across C123/C124/C127/registries; C127 X4 open. |
| `[VERIFIED]` -- direct read | C127 §2, §3 preamble **in full**, §3a, §4, §5a, §5b, §5d, §8's **two** carve-outs, X4; C123's `κ(t)` and its "separate, unanswered question"; C124's disposal 2 and F6-not-assessed; C128 §4, §5, §6 **in full**, §6a, §6b, §6d; C129 §2a/§3/§4's table/§6; C130 header **with "on `M_ι`"**, §0 C1, §3b, §5, §7, §9 W1 **verbatim**, §11 Pearl 2. |
| `[VERIFIED]` -- exact arithmetic, 21/21 gates, 2 headline-moving injections | §4a's lemma; §5c's `dim H²(P)`; §5d's `Pin` verdicts. |
| `[INFERRED]` | §3b's second `Ω₄` home; §3f; §4b's narrowed statement; §5a's Wang; §6's per-reading exclusions of `M_ι`. |
| `[CITED]` -- standard, not re-derived | Künneth; Whitney product formula; Wang sequence; `w(CP²)`, `w(RP⁴)`; Kirby-Taylor's `Pin⁺ ⟺ w₂ = 0` (via C129 §2a); **the APS spectral-flow identity (§6 S7's weakest link)**. |
| `[UNKNOWN]` | The KK-specific convention (§2c). Whether §6's surviving readings say anything about `t`. Whether C128-C130's work bears on OB1. `Ω^ξ_{14}` twisted. `G`. Round95. |

### Check (reproduces this decision)

```bash
cd experiments/20260902-c131-mapping-torus-dimension-consistency
python c131_dimension_consistency.py     # expect 21 / 21, ALL_OK = True
```

Independently of the code:

1. **C129 §4's table** -- `M_ι`: `ℤ, ℤ, 0, ℤ/2, **0**`; `S³×S¹`: `ℤ, ℤ, 0, ℤ, **ℤ**`,
   and C129's own *"the orientability difference"*. **This is the round.**
2. **arXiv:2512.17906v2 §4.1** -- *"`Z` with boundary `∂Z = ∂X`"*, `mod 1`,
   *"`Z'` is closed 4-manifold"*, *"the action of 3D Spin-Chern-Simons theory"*,
   *"independence on the choice of bulk"*.
3. **arXiv:1808.00009v3 §2.1.1** -- *"a fermion theory defined on some manifold
   `X`"*, *"`(d+1)`-dimensional Dirac operator"*.
4. `grep -n "X4"` in C127 -- *"C124 killed the 13D-covariant CS route. Does an
   `S³`-internal one survive F6?"*
5. **C127 §8** -- **two** carve-outs, with *mapping torus, spectral flow* in the
   second.
6. C127 §5b (`Ω₅`) against C127 §5d (*"is exactly A2"*, `Ω₄`) -- §3d.

**Falsifiers, stated so they can fail:**

1. **Exhibit a physical question of this project whose answer lives in
   `Ω^{Pin}_4`** -- i.e. on a closed **non-orientable** 4-manifold. Then `M_ι` is
   the right object, C129/C130 are directly on-target, and §6 is wrong. This is
   V3 and it is **the most likely way this round is wrong**.
2. **Show that `∫c₂` is definable on `M_ι`** -- e.g. via a twisted fundamental
   class. `H₄(M_ι;ℤ) = 0` says otherwise; `H₄(M_ι;𝔽₂) = 𝔽₂`, so a **mod-2**
   invariant survives, and whether that is enough for any reading in §6 is
   `[UNKNOWN]` and is the sharpest open sub-question this round leaves.
3. **Show C127 §4's list IS exhaustive**, contradicting §3b.
4. **Break the factorisation lemma** -- PART 1, with a `T³` control separating
   over `ℤ`.

---

## 10. What this round does NOT show

1. Does **not** resolve OB1 or move it out of `PARKED`; no mechanism, no
   `t`-selector, no anomaly evaluated.
2. Does **not** show that any of §6's surviving readings bears on `t`-selection.
3. Does **not** show that C128-C130's work bears on OB1 -- `[UNKNOWN]`, and
   **draft 2's claim that it does is withdrawn**.
4. Does **not** settle the KK-specific convention (§2c).
5. Does **not** re-derive the APS spectral-flow identity, or `w₁(M_ι) ≠ 0`.
6. Does **not** compute any bordism group.
7. Does **not** name `G`, touch round95, or choose a probe.
8. Does **not** read either paper in full; does not consult Freed-Hopkins,
   Witten 1982, Dai-Freed, Kirby-Taylor or ABP67/Gia71 directly.
9. Does **not** claim novelty for the mathematics (§10a).
10. Does **not** change `N_gen=3`, `lambda`, `sm_derivation_claimed`, or
    `safe_for_runtime`.
11. Does **not** edit `PARENT_ACTION_GATE.md`, `OPEN_BLOCKERS.md`,
    `null_results/`, `CLAIM_LEDGER.yaml`, or `pearl_registry/` -- the
    orchestrating session's. ⚠️ **`PARENT_ACTION_GATE.md:341-343` and
    `pearl_registry/INDEX.md:130` assert *"`Ω₄` is the home of a 3-dimensional
    theory's own anomaly, and this project has no 3D theory"*. The **conclusion**
    those rows support survives; **both stated premises do not** (§3b, §3c).
    **Per pass 3's D10 and §14, hold the edit until a fourth skeptic pass has
    reviewed this final draft.**
12. Does **not** solicit Tom Lawrence's Part 5.

### 10a. Novelty ledger

| Item | Status |
|---|---|
| §3a's orientability obstruction | **New to this project's record, and the round's principal content -- supplied by skeptic pass 3.** The underlying numbers are C129's own, printed and flagged a day earlier and never connected to relevance. |
| §3d's C127 `Ω₅`-vs-`Ω₄` inconsistency | **New. Unrecorded by any prior round or pass**; independently confirmed by pass 3. |
| §3b's second `Ω₄` home | **New to this project's record**; falsifies C130's first premise without restoring relevance. |
| §3c's `CS₃` false negative | **New**; falsifies C130's *verification* while leaving its premise true. |
| §6's seven-reading table with per-reading manifold check | **New as an enumeration**; two readings supplied by pass 2, the exclusion argument by pass 3. |
| §7(a)'s `S³×S¹`-vs-`M_ι` distinction as load-bearing | **New.** C128 §6a made the substitution knowingly; nobody had asked what it costs. |
| §2a's convention; §2b's construction; §4a's lemma | **Not new to the literature.** |
| **New *results* bearing on OB1's own question** | **Zero.** |

---

## 11. Proposed pearls (NOT written to `pearl_registry/INDEX.md` -- outside this round's brief, per C124-C130 precedent)

**Pearl 1 -- Pearl Gate. Before asking whether a manifold has the right
DIMENSION, ask whether it is an ELEMENT of the group in question.**
* **observation:** C130 asked "is `M_ι` the right dimension?" and three rounds,
  two drafts and two skeptic passes argued about degrees. The decisive fact was
  **orientability**: `H₄(M_ι;ℤ) = 0`, so `M_ι` carries no fundamental class,
  `∫c₂` is undefined on it, and it is an element of none of the candidate
  bordism groups -- **which are all groups of orientable, indeed Spin,
  4-manifolds.** Dimension is one membership condition among several, and it is
  the one people check.
* **falsifiable_prediction:** for every future "is this the right object?"
  question in this project, enumerating the **membership conditions** of the
  target group (dimension, orientability, tangential structure, bundle data) and
  checking each against the candidate settles it faster than arguing about
  degrees -- and at least one non-dimension condition will fail first. Confirmed
  once here; the numbers were already computed and printed.
* **impact_score:** 7. **trigger_condition:** any claim that a constructed
  manifold is or is not the right object for a bordism-valued question.
  **next_check:** 2026-12-01.

**Pearl 2 -- Caveat Gate. A phrase-grep cannot verify the absence of an OBJECT,
and a term shift can invert a round.**
* **observation:** C130 asserted *"this project has no 3D theory"*; draft 1
  `grep`-verified it against the **strings** `3d theory|3d qft|…` and tagged it
  `[VERIFIED-tool]`. The project calls the object `CS₃`. Draft 2 found that, and
  then **inverted the whole round** by treating a 3-dimensional *object*
  (a number, `∫_{S³}CS₃(ω^t)`) as a 3-dimensional *theory* (a QFT with a level
  and a partition function). **The grep was invalid AND the premise was true.**
* **falsifiable_prediction:** `[VERIFIED-tool, grep]` on a **negative existence**
  claim about a concept is wrong at a measurable rate; auditing this project's
  history for such tags will find more. Mandatory cheap fix: **name the object
  you expect to find and grep for THAT** -- and then check that finding it
  actually instantiates the concept, which is the step draft 2 skipped.
* **impact_score:** 7 -- it produced a false `[VERIFIED-tool]`, the premise is
  standing in two registry files, and the correction of it inverted a round in
  the wrong direction. **trigger_condition:** any negative existence claim
  verified by grep, or any argument turning on whether an object "is a theory".
  **next_check:** 2026-11-15.

**Pearl 3 -- methodological. Run the pass on the REWRITE, not only on the draft.**
* **observation:** passes 1 and 2 (concordant, `WEAKENED`) inverted this round's
  conclusion. A third pass, run on the rewrite specifically to probe
  over-correction, returned **`FALSIFIED`** and inverted it back -- on a fact
  (`H₄(M_ι;ℤ) = 0`) sitting in a table the rewrite cited. **Two concordant
  passes agreeing with each other is not evidence that the conclusion they
  produced is right; they reviewed the document that was wrong.** Each of the
  two turns was driven by a pass naming a one-line test it could not itself run.
* **falsifiable_prediction:** whenever skeptic passes cause a round to change
  its verdict *direction*, a further pass on the rewritten file finds a material
  defect. Testable by doing it; observed once here, and the defect it found was
  the round's headline. Cost: one pass. **The corresponding gap in
  `falsification-ladder.md`'s Paraphrase-Sensitivity Probe is that it specifies
  two passes on one document and is silent about a document rewritten between
  them.**
* **impact_score:** 7 -- it is a gap in the stack's own Step 8a protocol, not
  just in this round. **trigger_condition:** any round whose verdict direction
  changes in response to a skeptic pass. **next_check:** 2026-11-15.

---

## 12. Evidence tier of the central conclusion, and what would raise it

**Central conclusion:** *C130's W1 flag is correct about `M_ι` -- but neither of
its stated premises holds, and the operative obstruction is **orientability**,
not dimension. `H₄(M_ι;ℤ) = 0`, so `M_ι` carries no fundamental class and
`∫c₂` -- the number C127 §5d computed -- is undefined on it. Three readings do
make a 4-manifold built from the internal `S³` anomaly-relevant, and all three
produce C127 §5d's orientable `S³×S¹`, of whose bordism groups `M_ι` is not an
element. C128 §6's substitution is the step at which the object left them.
C128-C130's mathematics is untouched; its bearing on OB1 is `[UNKNOWN]` and this
round makes it less likely.*

* **`[VERIFIED]`** -- §3a's homology (C129's own table, direct read); §3d's C127
  inconsistency; §4a; §5's computations.
* **`[VERIFIED-tool, PRIMARY]`** -- §2a's convention; §2b's construction
  (`[WEAK]` transfer).
* **`[INFERRED]`** -- §6's per-reading exclusions of `M_ι`. **This is the step
  that carries the conclusion and it is not independently verified.** Each
  exclusion is a one-line membership check, but the *enumeration* of readings is
  not provably complete -- three drafts produced five, then seven.
* **`[UNKNOWN]`** -- everything in §10.

**Independent Verification Strength Ladder.** The decisive facts were **named by
three context-blind passes and verified against source by this session** --
still *"same model, isolated context" (Weak-Medium)*, however thorough, but the
turn-by-turn record (§0b) is unusually strong evidence that the question resists
single-pass judgement. The computational half is *"independently-written code"*
for its downstream arithmetic only; its inputs are `[CITED]`.

**What would raise it:**
* **A fourth pass on this draft** -- §14. The top item, and a prerequisite for
  the registry edit.
* **Attack §6's enumeration.** An eighth reading, or a physical question of this
  project whose answer lives in `Ω^{Pin}_4`, would overturn the conclusion (V3,
  falsifier 1).
* **Settle falsifier 2**: `H₄(M_ι;𝔽₂) = 𝔽₂`, so a mod-2 invariant survives on
  `M_ι` even though `∫c₂` does not. Whether any reading in §6 needs only the
  mod-2 version is the sharpest open sub-question this round leaves.

**What would NOT raise it:** more probes in §5d, more coefficient groups, or
more gates. The 21/21 is not evidence for the conclusion (§9).

---

## 13. Recomposition gate

Sub-claims: §2a's convention; §3a's homology; §3b; §3c; §6's exclusions; §4a's
lemma. **Their conjunction licenses:**

> *"`M_ι` is neither the `(d+1)` Dai-Freed mapping torus of any QFT this project
> names, nor an element of the bordism groups used by any of the three readings
> under which a 4-manifold built from the internal `S³` would be
> anomaly-relevant -- because it is closed and non-orientable. C127 §5d's
> `S³×S¹` is such an element; C128 §6 replaced one with the other."*

**It does not license** *"C128-C130's work is irrelevant"* (their object may yet
have a question of its own -- V3) nor *"the surviving readings say something
about `t`"* (untested; §10.2). The headline and verdict string are written to the
licensed statement.

---

## 14. Residual risk, stated as the round's top open item

**This final draft has not been skeptic-reviewed.** Passes 1-2 reviewed draft 1;
pass 3 reviewed draft 2; **draft 3 is unreviewed**, and it is the one that
inverts back. That is the same species as C128's finding C1 and this round's own
defect 10, and it is recorded rather than glossed.

**Recommendation to the orchestrating session, in order:**
1. **Run a fourth context-blind pass on this file**, prompted to attack §3a
   (does non-orientability really exclude `M_ι` from *every* candidate group, or
   only from the `∫c₂` one, given `H₄(M_ι;𝔽₂) = 𝔽₂`?) and §6's enumeration.
2. **Only then** correct `PARENT_ACTION_GATE.md:341-343` and
   `pearl_registry/INDEX.md:130` -- and correct the **premises** while keeping
   the conclusion, which is a different edit from either draft would have made.
3. Record Pearl 3's protocol gap (a rewrite between passes is not covered by the
   Paraphrase-Sensitivity Probe as written) wherever this project tracks
   methodology defects.
