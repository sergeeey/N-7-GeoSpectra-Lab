# C127 claim -- bordism/global-anomaly scoping for OB1 (Relaxation Map
item 8, the last-priority branch, attempted at explicit user request
after being flagged as low expected value)

## Question type (EstimandOps L0)
**Descriptive/existence, with an explicit Zero-Signal Gate check
required first.** Does a well-posed global-anomaly (bordism-invariant)
constraint exist that could select `t∈{0,1}`, or force the pair
together, for this project's actual frozen background? This is
NOT yet a computation -- it is first a SCOPING question: can the
entity, predicate, and measurable outcome even be named precisely
enough to compute, given what this project has and has not
established?

## Background, stated honestly before any computation

- (Kimi_Agent external document, 2026-09-01, cited not verified in
  this project until now) claims `Ω^{Spin}_{13}=0` (Anderson-Brown-
  Peterson) as a "no bordism invariant" argument -- **this is the
  UNTWISTED spin bordism of a point/manifold, not the twisted
  `Ω^{Spin}_{13}(BG)` a genuine global-anomaly argument needs.** A
  vanishing untwisted group does not by itself close the twisted
  question; it only closes the naive "just use the bare manifold's
  own bordism class" version.
- (C124, already established) **there is no independent 13D gauge
  field in this project's frozen content** -- `PARENT_ACTION_GATE.md`
  F1/F2/F5 name only a metric ansatz, a twist bundle on `S⁶` (itself
  constructed post-ansatz from `S⁶`'s own `SU(3)`-structure, not a
  fundamental 13D field), and Dirac operators. This is the single
  biggest obstacle to posing a bordism-of-a-classifying-space
  question at all: **`BG` for what `G`?**
- (C125, already established) this project's own audit
  (`SPIN13_TO_SPIN4_DECOMPOSITION.md`) states there is no established
  `Spin(1,12)` structure group and no consistent 13D parent theory is
  claimed -- so even the UNTWISTED `Ω^{Spin}_{13}` framing presupposes
  a global `Spin(13)` structure this project has not committed to.

## The Zero-Signal Gate check, required before proceeding

Per `falsification-ladder.md` Step -5: `(∃ entity) ∧ (∃ falsifiable
predicate) ∧ (∃ measurable outcome)`, all three required.

- **Entity:** candidate is "a bordism group `Ω^{Spin}_{13}(BG)` (or the
  relevant Pin/twisted variant) for a specific, named `G`." **NOT YET
  SUPPLIED** -- no `G` has been named by anything in this project or
  the external material. This round's FIRST job is to determine
  whether a `G` can be named at all from what this project has
  established, before any group is computed.
- **Falsifiable predicate:** "the relevant bordism group is nonzero,
  and its generator(s) correspond to a discrete choice matching
  `t∈{0,1}`."
- **Measurable outcome:** a computed (or cited, tool-verified) group
  value, and an explicit map from its generators to the `t`-family.

**If `G` cannot be named from this project's own already-certified
field content (per C124's own finding that no independent 13D gauge
field exists), this round should return `REFUSE(no_falsifiable_claim)`
or `BLOCKED (missing ingredient named)` -- NOT force a computation onto
whatever bordism group happens to be easiest to look up.** This is
explicitly permitted and is not a failure of the round.

## Falsifiable claim (only if the Zero-Signal Gate passes)

A twisted spin (or pin, if orientation-reversal per C125 is relevant)
bordism group `Ω^{Spin}_{13}(BG)` (or better-scoped variant) for a `G`
this project can actually name from its own frozen content is nonzero,
and a generator of it corresponds to a discrete choice that can be
mapped onto `t∈{0,1}` (or the pair, or a sign) without circularity
(not using the Dirac zero-mode count as input, matching this project's
own standing discipline for every OB1 F4 candidate).

**Kill criterion:** either (a) no `G` can be named (Zero-Signal Gate
fails, round REFUSEs/BLOCKS honestly), or (b) a `G` is named but the
relevant bordism group is zero or has no natural map to the `t`-family,
or (c) the argument requires importing a `G` not actually present in
this project's frozen content (an unmotivated addition, per
`PARENT_ACTION_GATE.md`'s own F1/F2 discipline against silently
modifying the background).

## What this round does NOT show

- Does not assume a positive result is likely -- the user was told
  explicitly, before this round was launched, that the naive version
  is already dead (`Ω^Spin_13=0` closes only the untwisted case) and
  that a real version requires literature-level work this round may
  not complete in one pass.
- Does not touch C123's `PARTIAL`, C124's `STRUCTURAL_NO_GO`, C125's
  `FALSIFIED`, or C126's `WEAKENED` verdicts -- independent question.
- Does not change `N_gen=3`'s CONDITIONAL status, `lambda=
  FREE_COUPLING_PARAMETER`, or `safe_for_runtime=False`.
- Does not solicit Tom Lawrence's Part 5.

## Verification plan

- Literature-first (per round86's own precedent): search for the
  actual, correct statement of what a "global anomaly" / bordism
  constraint would require for a Kaluza-Klein compactification with
  this project's specific field content (metric + torsion connection
  on `S³`, nearly-Kähler `S⁶` with an `SU(3)`-structure twist, no
  independent gauge field) -- before assuming any specific `G`.
- If a well-posed `G` and bordism group emerge, cite/compute and check
  against the kill criterion.
- If not, report `BLOCKED` or `REFUSE` honestly, with the missing
  ingredient named precisely (matching round86's own BLOCKED
  precedent), not manufactured into a false positive or a vague
  "further research needed."
- FL Step 8a skeptic pass on whatever this round produces, positive or
  negative, before it enters the permanent record.
